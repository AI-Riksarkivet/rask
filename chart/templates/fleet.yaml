{{- $root := . }}
{{- $secretName := .Values.existingSecret | default (printf "%s-app" (include "rask.fullname" .)) }}
{{- /* Plane ownership (lance-ns merge P4 — the render-time collision guard, risk 4): this template
     owns ONLY the rask fleet entries of .Values.services. The lance-ns lakehouse entries (catalog,
     lineage) are rendered BY NAME by templates/services.yaml under the same resolved fullname
     (`rask-<name>` — rask.fullname and lance.fullname both resolve to "rask" at install), so they
     are excluded here UNCONDITIONALLY: no service may render from two templates under any values
     combination. A new lakehouse entry in the services map must be added to this list; a new rask
     fleet entry needs nothing. */}}
{{- $lakehouse := list "catalog" "lineage" }}
{{- range $name, $svc := .Values.services }}
{{- /* `frontDoor: true` = part of the DEFAULT install, regardless of singleTenant. This gate used to
       name "gateway" literally, so every other front-door service was invisible unless someone set a
       LEGACY single-tenant flag. compute is the case that exposed it: it has an image, is built and
       imported by k3s-build/k3s-import, and the gateway routes /api/ray + /api/serve to its app-id
       UNCONDITIONALLY — so a default install answered ERR_DIRECT_INVOKE on the very route Makefile:406
       prints as its post-install check, and dev-frontends-k3s blocked forever polling it. compute is
       not a single-tenant concept; it is a client for a Ray dashboard URL, and Ray is external. */}}
{{- if and (not (has $name $lakehouse)) (or $svc.frontDoor $root.Values.singleTenant.enabled) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rask.fullname" $root }}-{{ $name }}
  labels:
    {{- include "rask.componentLabels" (list $root $name) | nindent 4 }}
spec:
  # hasKey+ternary, never `| default` — an explicit `replicas: 0` (scale to zero) is
  # falsy, so `| default 1` would silently resurrect the workload. Pinned by
  # tests/unit/test_invariants.py::test_no_numeric_helm_default_can_swallow_an_explicit_zero.
  replicas: {{ hasKey $svc "replicas" | ternary $svc.replicas 1 }}
  {{- if $svc.singleton }}
  strategy:
    type: Recreate
  {{- end }}
  selector:
    matchLabels:
      {{- include "rask.selectorLabels" $root | nindent 6 }}
      app.kubernetes.io/component: {{ $name }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $root.Template.BasePath "/configmap.yaml") $root | sha256sum }}
        {{- include "rask.daprAnnotations" (list $root $name $svc.port) | nindent 8 }}
      labels:
        {{- include "rask.selectorLabels" $root | nindent 8 }}
        app.kubernetes.io/component: {{ $name }}
        {{- include "rask.daprPodLabels" $root | nindent 8 }}
        lance.dev/logs: otlp # setup_otel builds a real LoggerProvider (5dae4538), so the file-tailed line is a dup — the uvicorn/crash tier is NOT dropped, see the filter's body guard
    spec:
      {{- with $root.Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{/* The drain budget, from the same `lifecycle` block as preStop and the sidecar block —
           see the arithmetic in values.yaml. These pods declared none, so they silently took the
           kubelet's 30s default: a number nobody chose and no comment describes, on pods that all
           run a Dapr sidecar whose flush the default does not budget for. */}}
      terminationGracePeriodSeconds: {{ $root.Values.lifecycle.terminationGracePeriodSeconds }}
      serviceAccountName: {{ include "rask.serviceAccountName" $root }}
      {{- $waits := list -}}
      {{- range $dep := ($svc.waitFor | default list) -}}
      {{- if and (eq $dep "minio") $root.Values.rustfs.enabled }}{{- $waits = append $waits $dep -}}{{- end -}}
      {{- end -}}
      {{- if $waits }}
      initContainers:
        {{- range $dep := $waits }}
        {{- if eq $dep "minio" }}
        - name: wait-minio
          image: busybox:1.36
          command: ["sh","-c","until nc -z {{ include "rask.fullname" $root }}-rustfs-io {{ $root.Values.rustfs.port }}; do echo waiting s3; sleep 2; done"]
        {{- end }}
        {{- end }}
      {{- end }}
      {{- /* THE ONE TEMPLATE THAT NEVER CALLED THIS, which mattered most here: the gateway is the
             only fleet service the prod overlay has ever run at replicas: 2, and it is the ingress
             every request in the estate passes through. Both pods on one node and the node's failure
             takes the estate down with a PodDisruptionBudget that permitted nothing — the exact
             failure the helper's own docstring records. Harmless at one replica. */}}
      {{- include "lance.spreadConstraints" $name | nindent 6 }}
      containers:
        - name: {{ $name }}
          image: {{ include "rask.image" (list $root $name) | quote }}
          imagePullPolicy: {{ $root.Values.image.pullPolicy }}
          command: ["uvicorn"]
          args:
            - {{ $svc.module | quote }}
            - "--host=0.0.0.0"
            - "--port={{ $svc.port }}"
            {{- include "rask.proxyHeaderArgs" $root | nindent 12 }}
            {{- range $a := ($svc.extraArgs | default list) }}
            - {{ $a | quote }}
            {{- end }}
          # Drain delay before SIGTERM: kube-proxy can still route to a terminating pod, so
          # without it a rolling update drops in-flight requests. Matters most on the gateway,
          # which is the ingress. Pinned by
          # tests/unit/test_invariants.py::test_every_first_party_deployment_is_hardened.
          {{- include "lance.preStop" $root | nindent 10 }}
          {{/* THE HARDENING values.yaml ALREADY CLAIMED THESE PODS HAD. Its comment says pod hardening
               is "applied to every app container via the lance.securityContext helper" — and this file
               did not call it, so the six fleet Deployments (gateway included, the pod the Ingress
               routes /api to) rendered with no securityContext at all while every lance-plane and web
               Deployment had one.

               The images already end `USER app`, so the missing `drop: ["ALL"]` was nominal — a
               container that never runs as uid 0 holds no effective capabilities either way. What was
               genuinely absent is what the CLUSTER enforces rather than what the image promises:
               admission-time runAsNonRoot, a RuntimeDefault seccomp profile, and a read-only rootfs. */}}
          {{- include "lance.securityContext" $root | nindent 10 }}
          {{/* And the scratch pair, so a read-only rootfs is SURVIVABLE: these images write to /tmp for
               OTel and pyarrow exactly as the lance ones do. Hardening without this crash-loops on the
               first spill instead of failing at deploy time. */}}
          volumeMounts:
            {{- include "lance.tmpMount" $root | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ $svc.port }}
          envFrom:
            - configMapRef:
                name: {{ include "rask.fullname" $root }}-config
            - secretRef:
                name: {{ $secretName }}
          env:
            {{- include "rask.otelEnv" (list $root $name) | nindent 12 }}
            {{- /* The storage registry, when a deployment declares one — the same rows explorer.yaml
                 renders, and for a second consumer: `ingest` resolves a run's declared source
                 `endpoint` against it (`ingest/objectstore.py`). Without it the fleet sees only the
                 built-in defaults, so every per-run endpoint override is refused as unregistered —
                 fail-closed, but a form field that can never be filled in. An unregistered endpoint
                 is refused rather than read from the deployment's own store, because a bucket of the
                 same name here would answer and the run would ingest the wrong bytes in silence. */}}
            {{- with $root.Values.storage }}{{- if .stores }}
            - { name: RASK_STORES, value: {{ toJson .stores | quote }} }
            {{- end }}{{- end }}
            {{- if $svc.rayClient }}
            {{- /* Ray token auth (gate 7 / R3): only the services flagged rayClient in
                 .Values.services (compute) get the pair — least
                 privilege; the gateway never talks to Ray. No-op unless
                 ray.auth.enabled (the include gates itself). JobSubmissionClient +
                 ray-kit's httpx paths read RAY_AUTH_TOKEN and attach the Bearer header. */}}
            {{- include "rask.rayAuthEnv" $root | nindent 12 }}
            {{- end }}
            {{- if $svc.lanceWriter }}
            {{- /* The S3 endpoint for services that write Lance. NOT optional: the pod already
                 receives AWS_* credentials from the fleet secret, so with no endpoint lance's
                 object_store silently resolves s3.us-east-1.amazonaws.com — the first in-cluster
                 ingest write got 403 InvalidAccessKeyId FROM REAL AWS, with the RustFS key echoed in
                 Amazon's error body. Credentials-present + endpoint-absent is the worst combination:
                 it fails far away, slowly, and names Amazon rather than the missing config. */}}
            - { name: RASK_S3_ENDPOINT_URL, value: {{ include "lance.s3Endpoint" $root | quote }} }
            - { name: AWS_ENDPOINT_URL, value: {{ include "lance.s3Endpoint" $root | quote }} }
            - { name: AWS_ALLOW_HTTP, value: "true" }
            {{- /* The warehouse root, from the chart's OWN bucket helper rather than a literal. The
                 first attempt hardcoded "rask-lakehouse" in values.yaml and RustFS answered
                 NoSuchBucket — the estate's bucket is `rustfs.bucket` (lance-catalog), and a values
                 file cannot call the helper because it cannot template at all. Deriving it here means
                 the ingest plane follows the bucket every other tier follows, including a rename. */}}
            - { name: RASK_INGEST_WAREHOUSE, value: "s3://{{ include "lance.stageBucket" (list $root $root.Values.medallion.producer.bronzeNamespace) }}/ingest" }
            {{- /* The CATALOG's own root, so the `lance-append` source can refuse a governed dataset.
                 `confine_to_lance_root` reads it as `if governed and _under(...)` — so when it is UNSET
                 the governed check does not fail closed, it is SKIPPED, and an operator who points
                 RASK_INGEST_LANCE_ROOT at the lakehouse bucket gets a second, unlineaged copy path
                 between governed tiers with no error. Measured unset on the deployed ingest pod
                 2026-08-23. Same expression as services.yaml's catalog row, deliberately. */}}
            - { name: LANCE_REST_ROOT, value: "s3://{{ $root.Values.rustfs.bucket }}" }
            {{- end }}
            {{- /* APP_API_TOKEN — ONE render site, TWO reasons a fleet service needs it, and they are
                 not the same reason. This block used to sit NESTED inside `if $svc.lanceWriter` above,
                 which tied the estate's service credential to whether a service writes Lance — two
                 unrelated facts. `notifications` is what exposed it: it writes no Lance and its bus
                 handler calls `assert_app_token_configured`, so it rendered with `RASK_DAPR_ENABLED=true`
                 and NO token and raised at import — a chart that renders green and a pod that never
                 starts. Un-nesting is the root fix; `ingest`'s render is unchanged (it now carries
                 `daprIngest: false` and reaches this block by the lanceWriter arm, on its original
                 condition).

                 The two arms, and why one condition cannot serve both:

                 `daprIngest` — the service HOSTS a sidecar-delivered route and FAILS CLOSED without the
                 token (`service_kit.governed.dapr_auth.assert_app_token_configured` raises whenever
                 RASK_DAPR_ENABLED is true and APP_API_TOKEN is unset). So it is gated on
                 `dapr.sidecars` ALONE: that key sets RASK_DAPR_ENABLED in the configmap AND renders the
                 Secret this reference reads, so the three move together and the pod cannot come up armed
                 without a key. Adding `auth.enabled` here would crash-loop the pod on an ungoverned dev
                 stack, which is the estate's DEFAULT combination.

                 `lanceWriter` — ingest's control door, where the token is a POLICY input rather than a
                 startup precondition: `authorize_ingest` reads an unset token as dev-open, so setting it
                 with no user door wired 403s every caller (measured live: every POST /api/ingests, from
                 the UI and a shell alike). Hence the extra `governedAuth => auth.enabled` arm — the
                 lock is armed only if there is also a key.

                 Setting BOTH flags on one service would render APP_API_TOKEN twice;
                 tests/unit/test_invariants.py::test_no_env_var_is_rendered_TWICE_on_any_workload is the
                 guard, so the interaction fails a gate rather than shipping a duplicate-key pod spec. */}}
            {{- $needsAppToken := false }}
            {{- if $svc.daprIngest }}{{- $needsAppToken = true }}{{- end }}
            {{- if and $svc.lanceWriter (or (not $svc.governedAuth) $root.Values.auth.enabled) }}{{- $needsAppToken = true }}{{- end }}
            {{- if and $root.Values.dapr.sidecars $needsAppToken }}
            {{- /* One secret for both arms, so the whole plane presents ONE service identity — the same
                 `{{ .Release.Name }}-dapr-app-token` the medallion producer, the movers and the
                 maintenance cron read. It cannot dangle: `dapr.sidecars` is what renders that Secret
                 (dapr-app-token.yaml's gating invariant) and it also gates this reference.

                 Worth keeping from the ingest incident, because it recurred here in mirror image: the
                 comment on the USER door below PREDICTED the 403 outcome in as many words, and the two
                 conditions still drifted apart — because nothing RENDERED them together. Prose next to a
                 condition does not hold two conditions in agreement; only one expression does. That is
                 why `$needsAppToken` is computed once above rather than repeated per arm. */}}
            - name: APP_API_TOKEN
              valueFrom:
                secretKeyRef: { name: {{ $root.Release.Name }}-dapr-app-token, key: token }
            {{- end }}
            {{- if and $root.Values.auth.enabled $svc.governedAuth }}
            {{- /* The USER door, for services that declare `governedAuth` — NOT every frontDoor
                   service. `frontDoor` means "the gateway routes to it", which is a different
                   question: the GATEWAY reads no auth settings at all, so keying on it emits
                   variables that service never reads. (`compute` was the second example here until
                   2026-08-26; it now declares GovernedAuthSettings and gates both routers, so it
                   carries `governedAuth: true` and this env is exactly what feeds its door.) `ingest.auth.authorize_ingest` refuses
                   the service-token path when the caller is the public gateway, and then has nowhere
                   to fall through to unless OIDC is wired — so closing that bypass without this left
                   a signed-in project admin with NO way in and the UI path dead rather than merely
                   ungoverned. IngestAuthSettings extends GovernedAuthSettings, so the prefix is
                   LANCE_ — verified, not assumed: emitting the wrong prefix produces variables the
                   service never reads, which looks wired and does nothing. */}}
            {{- include "lance.governedOidcEnv" $root | nindent 12 }}
            {{- include "lance.governedFgaEnv" $root | nindent 12 }}
            {{- with include "lance.governedFgaPins" $root }}{{- . | nindent 12 }}{{- end }}
            {{- end }}
            {{- /* Per-service env passthrough. Added 2026-08-03 because this block was a FIXED set of
                 includes: a values key a service needed had nowhere to render, and the first
                 in-cluster ingest run resolved a RELATIVE warehouse path inside the container as a
                 result — data would have landed in the pod's ephemeral filesystem and vanished with
                 it, silently, since a relative Lance path is perfectly valid.
                 Deliberately opt-in and per-service rather than a global map: config that applies to
                 everything belongs in the configmap this pod already mounts via envFrom, and a
                 catch-all here would become the place config goes to avoid a decision. */}}
            {{- range $k, $v := $svc.env }}
            - { name: {{ $k }}, value: {{ $v | quote }} }
            {{- end }}
            {{- /* THE EXTERNAL-BLOB DECISION HOLDS AT BOTH DOORS, from ONE value.

                 `services.yaml` renders this onto the catalog; without it here the ingest pod's
                 `approved_external_base()` returned None on every shipped chart, so every ingest
                 COPIED the corpus into the lakehouse instead of referencing it — measured in
                 `ingest/adapters.py` as 3,232 bytes external against 4,002,901 managed on a 4 MB
                 corpus, with one warning log and a green run as the only signal.

                 Rendered from `.Values.vending.externalBlobBases`, never duplicated into the
                 service's `env:` map: two literals is how the ingest side approves a write under a
                 base the catalog's manifest never registered, and `initial_bases` is create-mode
                 only — so that divergence cannot be repaired on the tables it lands on. The default
                 mirrors services.yaml's for the same reason. */}}
            {{- if eq $name "ingest" }}
            - { name: LANCE_EXTERNAL_BLOB_BASES, value: {{ $root.Values.vending.externalBlobBases | default (printf "s3://%s/models/" $root.Values.rustfs.bucket) | quote }} }
            {{- end }}
            {{- /* WHICH CHANNELS THIS DEPLOYMENT MAY SEND, derived from the SAME `channels.*.enabled`
                   flags that render the Components in notifications-channels.yaml.

                   Without this the two halves disagree silently and the feature is inert: enabling
                   `channels.email` produced a `bindings.smtp` Component, a secret reference and a
                   scope entry — and a service that never sent anything, because `RASK_NOTIFICATIONS_CHANNELS`
                   was read by settings.py and written by NOTHING in this chart. `channel_push()`
                   returns None on an empty list (proxies.py:181-183), so `fan_out` gets no push at
                   all and every channel send is skipped without a log line. An operator sees a
                   correctly-provisioned Component and no email, which is the worst shape of failure:
                   the configuration they touched looks applied.

                   DERIVED, never a second literal, for the reason the binding name three fields up
                   already carries: a hand-written `env.RASK_NOTIFICATIONS_CHANNELS` would be a
                   parallel source of truth that drifts the first time someone flips `enabled`. */}}
            {{- if $svc.channels }}
            {{- $chans := list }}
            {{- if (($svc.channels).email).enabled }}{{- $chans = append $chans "email" }}{{- end }}
            {{- if (($svc.channels).slack).enabled }}{{- $chans = append $chans "slack" }}{{- end }}
            - { name: RASK_NOTIFICATIONS_CHANNELS, value: {{ join "," $chans | quote }} }
            {{- end }}
          {{/* READINESS ASKS /readyz, NOT the badge. Both probes used to point at
               `healthPath | default "/api/health"`, and `/api/health` is a STATIC badge — compute's
               handler is `return Liveness()`. So the readiness probe answered 200 while the pod was
               draining, and the kubelet kept routing to it: the readiness contract inverted.
               `service_kit.probes` reports `starting`, `shutting_down` and a service's own hard
               dependency, and nothing asked it. `/api/health` is untouched — it is the
               frontend-facing badge. The three probes, the boot budget and the measured 5s timeout
               now live in ONE helper shared with controlplane.yaml, which is hand-written and had
               already drifted from this block twice. */}}
{{- include "rask.fleetProbes" $svc | nindent 10 }}
          resources:
            {{- /* A service may name its OWN tier (resources.<name>); otherwise the shared fleet one.
                 compute needs it — see the values comment: the Ray SDK does not fit in 512Mi. */}}
            {{- toYaml (index $root.Values.resources $name | default $root.Values.resources.fleet) | nindent 12 }}
      volumes:
        {{- include "lance.tmpVolume" $root | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "rask.fullname" $root }}-{{ $name }}
  labels:
    {{- include "rask.componentLabels" (list $root $name) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ $svc.port }}
      targetPort: http
  selector:
    {{- include "rask.selectorLabels" $root | nindent 4 }}
    app.kubernetes.io/component: {{ $name }}
---
{{- end }}
{{- end }}
