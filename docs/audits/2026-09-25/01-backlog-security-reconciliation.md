
## Phase 0: close what is already answered, and clear live residue (no code, no ruling)
WHY: These close or supersede rows today at almost no cost, and they remove live residue that later steps would otherwise misread. P0.5 comes before any row commit because the working tree already holds an uncommitted pylance 11 to 12.0.0 bump that someone else is making: 27 files, including catalog dataplane.py, lineage reconcile.py and maintenance floor.py/orphans.py, which LH-144, LH-141 and the purge-block row all rewrite. The domain probes were re-run on both 11.0.0 and 12.0.0 and hold on both, so anything filed upstream should name 12.0.0. Effort, honestly: Phases 0-6 need no ruling and total roughly 30 working days; Phases 7-8 add roughly 25 more once the rulings are in. Register edits ride the code commit of the row they belong to, never a bookkeeping commit of their own.
- **P0.1** [register; effort=under_an_hour; ruling=False; closes=LH-148, LIN-002, LH-041, XC-011; supersedes LH-077, XC-020, CP-047; deps=none]
  Close by register edit: LH-148 (the role-literal residue was retired by 7d retention; 0 of 278 dlq.lineage.events messages carry a role literal and the producers no longer mint one; its second question, whether a dropped table keeps provenance, moves to LH-144), LIN-002 (NominalTimeRunFacet is deliberately omitted for ARRIVAL-triggered runs only, the cascade and catalog writes, because OpenLineage defines it as 'AKA the schedule time'; the row says nothing about cron-bound runs), LH-041 (DECISIONS.md: a tag move is last-writer-wins because neither pylance 11 nor 12 has a conditional Tags.update; UpdateTableTag's code 14 means the spec permits a refusal but does not require one), and XC-011 (closed on the shipped content gate; nothing reads _control/bootstrap.json). Mark LH-077 and XC-020 superseded by the new transaction row, and CP-047 superseded by the chart row.
- **P0.2** [secrets; effort=under_an_hour; ruling=False; closes=XC-002; deps=none]
  XC-002: `kubectl delete secret rask-ray-compute-s3`. Its own last-applied annotation is the only base64 copy, and it has no owner or reader. Do NOT re-apply deploy/ray-lance-demo.yaml: it is a hard overwrite that pins main-88ccfa2c while the live head runs main-cda85df4. Record that the verbatim owner rule ('ESO for a pod without a sidecar, never through env') requires ESO-to-file delivery; do not add that as mechanism text to DECISIONS §B. Rewrite tests/unit/test_the_ray_credential_has_one_source.py's docstring, .claude/skills/rask-dapr/SKILL.md:150-153 and rask-helm §7 row 1 to match. Hand the Ray head's S3 PAIR (S3_KEY and S3_SECRET) to LH-129, its service tokens to LH-160 and HF_TOKEN to the HF row. Then close.
- **P0.3** [secrets; effort=under_an_hour; ruling=False; closes=NEW Job-literals row (husk half); deps=none]
  Delete the 19 credential-bearing husk Jobs with no TTL: rask-minio-scoped-users-r146..r163 (17), rotate-scoped-users, and rask-openbao-seed-r3. No spec references any of them. Every past release revision still holds the same literals, so the rotation in P4.4 is still required.
- **P0.4** [boundaries; effort=under_an_hour; ruling=False; closes=LH-099; deps=none]
  LH-099: observe one compaction RunEvent on lineage.events.v1, driven from the 240 MiB/60-fragment fixture LH-183 already built. Emission is enabled, but none has been seen recently because versions_removed=0 estate-wide. Then record the decline in DECISIONS.md on the corrected basis: a compaction changes no object's identity, name, protection, refs or grants; spec.yaml has no compaction operationId; GetTableStats exposes only FragmentStats. Close. Split the purge-block defect into its own row so it is not lost.
- **P0.5** [register; effort=under_an_hour; ruling=False; closes=none (precondition for every code step); deps=none]
  Whoever owns the uncommitted pylance 12.0.0 bump lands it or shelves it before any row below commits to those files. Two things to know: the root venv was re-synced to it by a review probe, and the 11 lakehouse deployments run lakehouse-854a0cf2.
- **P0.6** [chart; effort=under_an_hour; ruling=False; closes=none (corrects the evidence the chart ruling D11 rests on); deps=none]
  Commit .claude/skills/rask-helm/ (SKILL.md and NOTICE, both untracked). Correct its 'hit the ceiling twice' to four hits (2026-08-15, 2026-08-18, 2026-09-21, 2026-09-24). In the same commit, rewrite the scripts/helm.sh header and the 2026-08-15 DECISIONS entry. Both still blame chart/charts/*.tgz, which Helm never stores. What fills the release object is template source, including about 299 KB of template comments, plus Kueue's rendered CRDs.

## Phase 1: foundations every later phase stands on
WHY: Each step here is a precondition for several later ones. The FGA model-versioning fix (P1.2) must land before ANY model.fga change (P2.1, P2.2, P2.5, P8.2), because today every non-catalog service pins whichever model was newest at its own boot. A repointed relation can therefore be missing from the model a lineage pod checks against, and batch_check (before P1.1) reports that as a silent denial. The seed hook (P1.3) must land before any change that adds a store property (P3.3 digests, P4.3 bundle split, P4.4 generation): until then a new pod can start before the value it reads exists. The outbox fix (P1.5) and the dropped_at fix (P1.6) are live data-loss and data-growth defects that LH-064's flip and LH-144's residue work build on.
- **P1.1** [authz; effort=under_an_hour; ruling=False; closes=NEW batch_check drops the per-item error; deps=P0.5]
  packages/service-kit/src/service_kit/governed/fga.py:879-881: _do_batch_check raises when any per-item r.error is set, so the existing _guarded retry and ServiceUnavailableError path treat it exactly like check(). Write the RED test first with a fake client that returns allowed=False plus a CheckError. Keep the {request.object: bool} contract: the SDK already maps each result back to its request, so no correlation_id re-keying. notifications gets the fix only when its Phase 3 image is rebuilt.
- **P1.2** [authz; effort=a_day; ruling=False; closes=NEW model hook compares relation names only and services pin the newest model; deps=P0.5]
  FGA model versioning. write_model.needs_write compares fga._canonical_model bodies, not relation names. The model hook and scripts/fga-store-check.sh pick the store named lance-catalog (newest created_at), not stores[0]. fga.resolve() pages the store's models and returns the newest whose canonical body equals the bundled model.json; it retries with a bounded wait, then fails closed. Move the openfga-model hook to pre-upgrade (keep post-install for first install only). Models are immutable and old pods keep resolving their own body, so writing early is safe, and the new pods then never crash-loop waiting on the catalog. RED test beside test_the_model_is_written_only_when_it_differs.py: a body-only tightening must set needs_write.
- **P1.3** [secrets; effort=hours; ruling=False; closes=XC-005; deps=none]
  XC-005: the one OpenBao seed Job template becomes an idempotent hook on UPGRADE at BOTH pre-upgrade and post-upgrade, following the minio-scoped-users precedent. The post-upgrade run is needed because dev OpenBao is `server -dev` in memory: a pod restart after the pre-upgrade seed would leave the store empty. On install it stays an ordinary resource. Weight it before minio-scoped-users, use delete-policy before-hook-creation, and bound it with lance.bootstrapJobDeadline. Do not duplicate the template, because hooks are stored in the release object.
- **P1.4** [secrets; effort=under_an_hour; ruling=False; closes=NEW nothing installs the external-secrets operator; deps=none]
  Add `make k3s-eso`: scripts/helm.sh upgrade --install external-secrets (pinned 2.10.0) into namespace external-secrets, as its own release and never a subchart, and make it a prerequisite of k3s-up. Install the same operator in scripts/ray_e2e_stack.sh and the e2e-py stacks, so that P4.5 can later flip the default without breaking them.
- **P1.5** [provenance; effort=hours; ruling=False; closes=NEW outbox relay deletes every staged DatasetEvent; deps=P0.5]
  services/lineage/src/lineage/api/reconcile_cron.py:526 currently deletes every staged DatasetEvent as poison. Parse staged bytes with the consumer's own discriminator (consumer._parse), and route DatasetEvents to repository.ingest_dataset_event and RunEvents to ingest_event, both still through enforce_bus_authz. RED test first, driven by the catalog's real build_write_event(create_table). Rewrite the outbox prose in the same commit: service-kit outbox.py:1, values.yaml:479 and :1552, and the _send comment. They should say the outbox NARROWS the loss window to commit-to-stage, not that it closes it.
- **P1.6** [provenance; effort=hours; ruling=False; closes=LH-144 (mechanism half; residue in P3.6); deps=P0.5]
  LH-144 root cause, a live regression since LIN-004 (64baf2f0, 2026-09-23). The catalog emits drop_table as a DatasetEvent, and dropped_at (cypher.py:222-223) reads only (:Run)-[:WROTE] history, so no catalog drop has been recognised since that date. Every dropped table then turns UNGOVERNED once its tuples are revoked. Fix: derive dropped_at from DatasetEvent DROP lifecycle rows too, ordered by event_time, so that a recreate clears it and it stays derived, never a mutable flag. Also make sure a maintenance compaction recorded after a drop does not un-drop the table (live example: acme-bronze$stockprobe_91c41a73). Write the RED test with the real build_write_event(drop_table) through ingest_dataset_event. Rewrite the stale tables.py:577 comment. Separately, re-probe `exists` on csx1ns$r1 against the deployed catalog: the row recorded a 200, but the code gates exists on can_get_metadata, so record which tuple admits it.

## Phase 2: catalog authz, spec conformance and vending scope (no ruling)
WHY: These are pure catalog/service-kit changes that need no ruling, and each is independently deployable. P2.1, P2.2 and P2.5 change model.fga, so they come after P1.2. P2.6 needs P1.1's error semantics. P2.8 takes its subdirectory set from P2.7's measurement rather than from a hand-written list. P2.9 bundles every vending-module change into one build and one deploy, and its RegisterTable guard is also what XC-017 §B7 needs to see refused. Until LH-197 lands, every deploy here rolls all 11 lakehouse deployments, which is why D14 should be confirmed today.
- **P2.1** [authz; effort=hours; ruling=False; closes=NEW transaction doors check a table-scoped id against a namespace object; LH-077; XC-020; deps=P1.2]
  Re-scope both transaction doors to table:<id minus its last segment>: describe needs can_get_metadata, alter needs can_write_data. Answer a one-segment id with InvalidInputError before FGA, which matches the backend's own answer. Refuse alter with UnsupportedOperationError while the dir backend answers SUCCEEDED but applies nothing (re-verified on pylance 11 and 12), and file that upstream naming 12.0.0. Delete `type transaction` from model.fga and model.fga.yaml, keep `fga model test` green, regenerate model.json, and move the two test_authz cases to the table-scoped id shape.
- **P2.2** [authz; effort=hours; ruling=False; closes=LH-076 (all but the tenant-creation door); deps=P1.2]
  On `type estate`, add computed relations, all `owner`: can_list_all_projects, can_manage_stores, can_administer_authz and can_observe_estate_lineage. Repoint stores.py, access_admin.py, me.py, the project listing and lineage's api/fga_deps.py:169 to them, and keep can_observe_events for GET /v1/events only. POST /v1/projects is left UNTOUCHED until D2. Update the model.fga.yaml estate block and regenerate model.json. No behaviour changes, because every new relation is `owner`, which is what the doors check today.
- **P2.3** [authz; effort=hours; ruling=False; closes=LH-150; deps=P0.5]
  LH-150: delete the five delimiter settings (catalog LANCE_NS_DELIMITER, ingest RASK_CATALOG_DELIMITER, MAINTENANCE_DELIMITER, MEDALLION_DELIMITER, MEDIA_CATALOG_DELIMITER), their chart lines (services.yaml:74), the compose line and the seed-script override. Every service then uses service_kit.lakehouse.naming.CATALOG_DELIMITER. Record in DECISIONS.md, citing spec.yaml components/parameters/delimiter ('When not specified, the `$` delimiter must be used'). delimiter.py's refusal stays.
- **P2.4** [authz; effort=hours; ruling=False; closes=LH-037; deps=none]
  LH-037, answered by the spec. (1) drop_namespace: on NamespaceNotFound after the gate passed, run the idempotent trailer (revoke_ownership, clear_protection, unbind_namespace, delete_policy), then answer Skip with 204 and Fail with the spec error. (2) create_namespace Overwrite with Restrict semantics, composed in rask as drop-then-create because the dir backend implements no Overwrite. An EMPTY namespace requires can_delete on it in addition to create-on-parent, and has its tuples revoked and the caller seeded. A NON-empty one is refused with NamespaceAlreadyExists (code 2), a code CreateNamespace allows, with a detail naming its contents. Record both in DECISIONS.md.
- **P2.5** [authz; effort=a_day; ruling=False; closes=NEW can_get_metadata recurses down through children; deps=P1.2]
  Port the upward-visibility fix in the DECISIONS.md:2253 shape, not the reconciliation's inverted one. Bare relation names stay the directly-assignable grant relations and no tuple is rewritten. Add computed *_effective twins (for example reader_effective: reader or writer_effective or reader_effective from parent) and repoint the checks to them. Add visible_below: reader or writer or owner or ... or visible_below from child, over the bare names, and set can_get_metadata: reader_effective or visible_below from child. Keep the deep-grant breadcrumb case green. Re-measure the denied describe on warehouse:acme-bucket before and after (the audit measured 1.146 s against 0.110 s). Re-run the weighted-graph audit and correct values.yaml:3050, which says the model has no `and`/`but not`.
- **P2.6** [authz; effort=hours; ruling=False; closes=NEW listings filter through one estate-wide list_objects; deps=P1.1]
  Listings (tables.py:220-236 and the warehouse, namespace and model listings) stop filtering through one estate-wide list_objects, which is capped at 1,000 objects per subject. Positive short-circuit: the caller's downward-inherited `reader` on the parent skips per-item checks. Negative short-circuit: can_get_metadata false on the parent returns an empty list. Otherwise run batch_check over the candidate page. Never use can_get_metadata as a positive short-circuit: it is upward visibility and would list every sibling. Keep list_objects only where there is no candidate set (/v1/me).
- **P2.7** [authz; effort=a_day; ruling=False; closes=NEW write-tier vend exceeds the FGA rungs (the _refs and Delete half); deps=none]
  Measure first. Write a RED test that drives pylance append, overwrite, index build, compaction and cleanup under a narrowed session policy against MinIO, and records which prefixes each one writes and deletes. Then vend writer paths only: Put on data/, _deletions/, _transactions/, _indices/ and _versions/; nothing on _refs/*; DeleteObject only when the granting rung is can_maintain (credentials.py knows granted_by). Record the residue as the format's: a session policy cannot refuse a Put that overwrites a manifest. The tree/* half waits on D3.
- **P2.8** [authz; effort=hours; ruling=False; closes=NEW branch-scoped credential reaches nested branches; deps=P2.7]
  Branch prefixes. At branch create, refuse any segment equal to a reserved layout name (_versions, _transactions, _deletions, _indices, _refs, data, tree) with InvalidInput. State it as a vend-scope layout constraint rask owns, NOT a second copy of Lance's grammar (the dataplane.py:1976 docstring forbids that), and file the ambiguity upstream on pylance 12.0.0. Scope the branch statement to tree/<b>/{P2.7's measured set}/* so a descendant branch falls outside it. Also serve the branch on the spec's own door: honour DescribeTableRequest.branch together with vend_credentials instead of refusing it, so a stock lance-namespace client can get a branch-scoped credential.
- **P2.9** [sts; effort=a_day; ruling=False; closes=NEW RegisterTable attaches any location; NEW warehouse endpoint redirects opens (refusal half); NEW `${` policy variable; LH-072; XC-017 §B7; deps=P0.5]
  One catalog vending batch, one build, one deploy. (a) RegisterTable guard: before native.call, refuse a location that starts with a control prefix or the models registry, or that equals, contains or sits inside ANOTHER id's live table location. Same id at the same location falls through, so the medallion head's converge arm keeps working. Refuse with InvalidInputError (13) or PermissionDeniedError (15), never a 409, because register_written_dataset reads any 409 as convergence. The defect statement adds that drop plus purge of the thief's id destroys the victim's bytes. Append an explicit Deny on _projects, _protection, _warehouses, _tasks, _transforms, _gates, __manifest and _policies/<kind>/ to the TABLE vend paths only (credentials.py:159, describe at tables.py:462); leave _lineage_outbox and _validate untouched. (b) Refuse every non-estate warehouse endpoint at create, and fail closed at open for every foreign-endpoint record with or without a ref, until a ref is consumed end to end. (c) Refuse `{` and `}` in any segment at create; add `${` to the vending-side prefix guard only; correct the 'no escape' sentence. (d) A redacting SecretStr-style value type for aws_secret_access_key and aws_session_token inside VendedCredentials.storage_options, covering repr, str, model_dump and error echoes. The wire stays the spec's flat map. Prove (a) live with values that MUST fail: another id at an existing table's location, `_projects`, a relative `..` path and a second base, all refused.
- **P2.10** [sts; effort=hours; ruling=False; closes=LH-177 (endpoint-split half); NEW SSE-KMS row stays parked; deps=P2.9]
  Split the STS-call endpoint from the client-facing endpoint with a distinct vending.stsEndpoint chart value. This is needed on managed S3 whatever D9 decides, because services.yaml:166 sets the STS endpoint to the S3 endpoint and config.py refuses an empty one. Park the SSE-KMS kms:* statement behind this split until an AWS deployment exists; do not add the boot refusal nobody asked for.

## Phase 3: provenance, making the bus signature mean something (no ruling)
WHY: The flip (P3.5) must be the last step, and four things must be true before it. First, the outbox must stop destroying DatasetEvents (P1.5). Second, medallion and maintenance signing must have been SEEN live; today only the catalog has been observed signing. Third, the signature facet must be in its final signed shape, with a key id, so that a rotation never refuses in-flight or staged events; landing the kid before the flip means the flip ships the final facet. Fourth, no bus producer may hold another producer's signing key. The live lance-config-catalog denies no service token, so the catalog could today self-sign as service-bronze-to-silver, and a delegator allowlist does not touch that. P3.3 therefore needs P1.3: the digests it reads must exist before the new catalog pods start.
- **P3.1** [provenance; effort=hours; ruling=False; closes=none (precondition of LH-064's flip); deps=none]
  Restart each producer pod, then drive one POST /produce cascade and one maintenance emit. In public.lineage_events, read a signed event with no delegation from each of service-bronze-to-silver, service-silver-to-gold, service-media-to-silver and service-maintenance. Pre-register the field list before running: signer, onBehalfOf, and whether a signature is present.
- **P3.2** [provenance; effort=hours; ruling=False; closes=NEW signature has no key id and custom facets are unversioned; deps=P0.5]
  lineage-kit signing.py builds the facet through service_kit custom_facet, so it carries _schemaURL, and adds `kid` and `canon: 1` INSIDE the signed facet. The lineage resolver accepts the current key plus, during a rotation window, the previous one. At the same time, publish versioned JSON Schemas for the author, lance and signature facets and stamp each one's _schemaURL: additive changes are a minor bump, renames or retypes a major bump, and no fixture gate is added.
- **P3.3** [provenance; effort=a_day; ruling=False; closes=NEW the catalog reads every producer's signing key; deps=P1.3]
  The catalog's service door (api/security.py:117-128) verifies a caller's bearer against a digest instead of reading the raw service-token-<identity>. The seed writes each digest to a catalog-readable path (create-if-absent, in-cluster). lance-config-catalog then denies every peer's service-token-*, like every other producer does. Only the owner and lineage, the accepted symmetric verifier, still read a producer's key.
- **P3.4** [provenance; effort=hours; ruling=False; closes=NEW Ray head holds every stage runner's signing key (the unused three); deps=none]
  Delete the three stage-runner RASK_LINEAGE_TOKEN_SERVICE_* entries from chart/templates/_ray-cluster-config.tpl and deploy/ray-lance-demo.yaml. No job reads them: ray_stage_job.py reads no lineage token, and ray_train_job.py:202 is the only reader. Keep only the trainer's until LH-129. Read the deployed image before applying the demo manifest, because applying it is a hard overwrite.
- **P3.5** [provenance; effort=a_day; ruling=False; closes=LH-064; deps=P1.5, P3.1, P3.2, P3.3]
  LH-064 flip bundle, RED tests first. (a) Delegator allowlist: LINEAGE_DELEGATING_SIGNERS, derived by the chart from catalog.serviceIdentity, fail closed when empty, enforced in enforce_signature_if_present so the bus and the outbox drain share it. (b) An unsigned bus event is an ack-and-count unrepairable refusal (reason=unsigned) and never parked, so a restart over the 9,119 retained LINEAGE messages parks nothing. (c) Replay is a FALLBACK: catch the signature PermissionDenied, then admit only an event byte-identical to recorded_event(event_identity(...)). The signed path pays no extra read. (d) Producers stop degrading to unsigned: do not cache an empty key; re-resolve, and leave the event staged in the outbox for a signed retry. (e) Switch verify-if-present to require-a-signature. (f) Rewrite the channel, signature, _StampedAuthor, 'EMPTY IS A REAL ANSWER' and DaprEmitter prose. Reword closes-when to: refused at /lineage-events and in the outbox drain; delegation only from the chart-derived set; a restart parks nothing; notifications' bus lane recorded as unverified; the Ray-lane identities bounded until LH-129. Deploy by building each changed stem with `dagger call image --name=<stem> publish`, then scripts/helm.sh --reset-then-reuse-values --set image.tags.<stem>=<tag>; read back each pod's image and `helm history rask`. Prove it with a control pair: restart lineage and see zero new dlq.lineage.events; publish one forged unsigned event and see it refused.
- **P3.6** [provenance; effort=hours; ruling=True; closes=LH-144; deps=P1.6, D14]
  After P1.6 is deployed, re-count lineage_reconcile_ungoverned. For the pre-LIN-004 remainder, the reconcile records an observed-absence DROP (a DatasetEvent with lifecycle DROP, never a synthetic Run) only on a clean dataset-not-found from read_storage_version, never on NoSuchBucket or access denied. Anything whose bytes are present is reported as ungoverned_live. Restate closes-when as: no ungoverned node names absent bytes, and ungoverned_live names only fixtures and LH-164's table. The storage read runs on lineage's static key; that key's replacement is in P8.9.
- **P3.7** [provenance; effort=hours; ruling=False; closes=LH-141; deps=P1.6]
  LH-141: a one-shot, admin-gated maintenance door whose input comes from the AGE query MATCH (d:Dataset) WHERE NOT d.source_uri CONTAINS '://' (60 nodes), not from the reconcile's 23-name report. For each name it resolves DescribeTable(with_table_uri=true) through the catalog and emits build_restamp_event through the signed emitter. For datasets the sweep still holds, it also repairs the Lance-side lineage.dataset_id stamp with update_schema_metadata, which is metadata-only and keeps _rowid; one example is s3://acme-bucket/4750a5b9_acme-bronze$events. First confirm that the restamp operation's rung is one service-maintenance holds. Run it once. Expect exactly the two ungoverned removals left, and zero location-mismatch refusals across a full tick.
- **P3.8** [provenance; effort=hours; ruling=False; closes=LH-171; deps=none]
  LH-171: list_specs reports each malformed record's key and the legacy lane/name read off the raw JSON. Delete exactly those records through POST /v1/project/{id}/transform/delete with name=<legacy lane>; the legacy key sha256(project:lane) is the same derivation. Take care to keep the one valid record. Read the transform.specs.malformed gauge at 0 and close. No alert and no gate.
- **P3.9** [provenance; effort=hours; ruling=False; closes=NEW crash between commit and outbox stage loses author and inputs; deps=P1.5]
  Low priority. On the pylance paths that accept transaction_properties/commit_message (write_dataset and commit, i.e. creates and appends), stamp run_id and the author sub. The reconcile back-fill reads them through read_transaction and attributes the write instead of claiming it. State the partial coverage in the prose.

## Phase 4: secrets out of env and out of the release object (no ruling)
WHY: This is ordered by what each credential's second reader needs. The bundle split (P4.3) needs the seed hook (P1.3) and the catalog digest door (P3.3), otherwise the catalog cannot deny its peers. In-cluster generation (P4.4) needs the ESO operator codified (P1.4) and the split. The default flip and the no-store render `fail` (P4.5) come last because the e2e lanes deploy with openbao OFF on purpose, and failing their render first would red CI. The release has 38.8 KB free, so every step here must be net-zero in rendered bytes until the Kueue ruling (D11) lands. The deletions (value-rendered Secrets, seed literals) go first or in the same commit as the additions, with the delta measured the rask-helm §1 way.
- **P4.1** [secrets; effort=under_an_hour; ruling=False; closes=XC-036; deps=none]
  XC-036: add a render-time `fail` that compares openfga.datastore.existingSecret and greptime's existingSecretName with printf "%s-..." .Release.Name. Also note the greptime `endpoint: http://rask-minio:9000` hardcode in the row.
- **P4.2** [secrets; effort=hours; ruling=False; closes=NEW every daprd authenticates to OpenBao as root; deps=P1.3]
  Dapr sidecar's OpenBao token becomes non-root in both modes. The seed authenticates with Kubernetes auth (its SA JWT against a narrow provisioning role), not BAO_TOKEN=root as literal env. A writer re-mints a policy-bound sidecar token (read only the paths lance.secretScopes' apps fetch) before its TTL and writes <release>-openbao-token: ESO VaultDynamicSecret with refreshInterval shorter than the TTL, then a sidecar restart. Move ESO's kubernetes-auth provisioning out of the devMode-only block. Drop 'revoke root': dev mode recreates it on every restart.
- **P4.3** [secrets; effort=a_day; ruling=False; closes=NEW shared secret/lance bundle readable by every sidecar'd app; deps=P1.3, P3.3]
  Split secret/lance per consumer, the way XC-072 split the tokens. Move BOTH halves of each S3 pair (access-key-id and secret; today the id is a chart value) into secret/<identity>-s3. Put the root pair, postgres and publisher material on paths only ESO's policy and their single consumer read. Keep defaultAccess allow (the registry-driven readers need it) and add the new root, peer, postgres and publisher paths to every app's deniedSecrets. Update the seed, the ESO remoteRefs and the ESO policy in the same change.
- **P4.4** [secrets; effort=multi_day; ruling=False; closes=NEW every dev secret is created by the chart render; NEW HF token as a chart value; NEW ray-auth-token written by nothing; NEW Job specs carry credentials as literals (remainder); XC-004 (all but the default flip); deps=P1.4, P1.3, P4.3]
  Generate secrets in the cluster, never in the render. The seed goes create-if-absent (`bao kv get || bao kv put <f>=$(head -c 32 /dev/urandom | base64)`), or ESO's Password generator plus PushSecret is used, for dapr-app-token, frontend-session, ray-auth-token, the six scoped S3 secrets and the service tokens. ExternalSecrets write <release>-dapr-app-token, -frontend-session, -ray-auth-token and -hf-token. Delete dapr-app-token.yaml, frontends.yaml's Secret, secrets.yaml's HF_TOKEN, the static infra, observability and ray-auth Secrets, and every `{{ .Values.* }}` literal in the seed command. Remove --set dapr.appToken, ray.auth.token, sessionSecret and hfToken from values-prod.yaml and the Makefile. A `make` target writes the HF token straight into OpenBao, and it is mounted at a dedicated path with HF_TOKEN_PATH, not inside HF_HOME's cache; verify that a Serve replica sees it. The scoped-users hook reads its six secrets from a mounted ESO file, publishing the keys one release before consuming them. This is an interim that removes a publicly derivable key (sha256 of 'rask-<id>-s3-minioadmin'); it is not the fix for static users (P8.9). Then rotate everything, including the HF token at huggingface.co, because all 10 kept revisions hold the old values.
- **P4.5** [secrets; effort=a_day; ruling=False; closes=NEW no secret store renders plaintext S3 env; XC-004; deps=P4.4]
  Fix the install-time seed-versus-reader race, then move scripts/ray_e2e_stack.sh, tests/e2e-py/test_governed_union_e2e.py and test_media_e2e.py to openbao-on plus ESO. Only then replace every `if not lance.secretsViaDapr` S3-secret env branch with one render `fail` ('no secret store configured; secrets never ship in env'), and flip externalSecrets.enabled to true in values.yaml. Update the five unit renders that pass openbao off.
- **P4.6** [secrets; effort=hours; ruling=False; closes=NEW store-fetched secrets cached for the life of the process; deps=none]
  Secret caches: a bounded TTL with jitter and single-flight on warehouse_credentials.resolve and on the service-token bundle, still failing closed on a miss. The APP token instead accepts current OR previous and rotates by restart, because daprd keeps its APP_API_TOKEN until the pod restarts and a refreshed app would 403 its own sidecar. Document warehouse rotation as rotate-by-new-reference.
- **P4.7** [secrets; effort=hours; ruling=False; closes=LH-160 (sidecar and gate halves); deps=P4.3]
  LH-160 sidecar half. Add compute to lance.secretScopes (after the split, so it does not gain root) and move APP_API_TOKEN and RAY_AUTH_TOKEN to the store, observing a real sidecar fetch. In the same commit, widen the existing ratchet walk as the row's closes-when asks: any pod template (RayCluster and RayService groups included), initContainers, and deploy/*.yaml, rendered in both the values-local and prod shapes. Name the injector- and operator-added env as recorded residue: daprd's APP_API_TOKEN from dapr.io/app-token-secret, and KubeRay's RAY_AUTH_TOKEN. Set the baseline to the true count and restate the docstring as §B's posture question.
- **P4.8** [secrets; effort=hours; ruling=False; closes=XC-001 (zone half); LH-160 (zone entries); deps=P4.4]
  Zones read SESSION_SECRET and OIDC_CLIENT_SECRET from mounted files through readSecretFile. When OIDC is configured and the file is unreadable, answer 503, never an unsealed cookie. Hold a two-key SESSION_SECRET ring: seal with the newest key, open with current or previous. Add both names to SECRETS_DELIVERED_AS_FILES in frontend/packages/zone-contract/src/secret-from-file.test.ts, an existing list.
- **P4.9** [secrets; effort=a_day; ruling=False; closes=LH-160 (infra entries); deps=P4.7]
  Give the infra images file-based credentials: MinIO MINIO_ROOT_*_FILE, AGE POSTGRES_PASSWORD_FILE, an OpenFGA config file, and the Collector's ${file:...}. Verify each image's support in the session before relying on it, and record any that cannot as a named exemption.
- **P4.10** [secrets; effort=a_day; ruling=False; closes=XC-003; deps=P4.4]
  XC-003: generate a writer password and a read-only password in-cluster (P4.4's mechanism). An ExternalSecret renders `passwd`, mounted through the subchart's extraVolumes. Set GREPTIMEDB_STANDALONE__USER_PROVIDER=static_user_provider:file:<mount>/passwd (a path, not a secret) and leave auth.enabled false. Confirm GreptimeDB 1.1.1's per-user read-only mode before splitting reader from writer. Wire the five real consumers by file: the Collector, the TTL hook, vmalert -datasource.basicAuth.passwordFile, Perses, and home's audit viewer. Closes when an unauthenticated in-cluster DELETE is refused; that is also the append-only half of XC-017 §B10.

## Phase 5: storage on vended sessions, the parts that need no ruling, plus the probes the redesign depends on
WHY: The medallion (P5.1) and maintenance (P5.2) changes remove fallbacks to static keys on WRITE paths. The owner's rule bans those outright, and both are preconditions for ever deleting those MinIO users. The web-identity probes (P5.3) run BEFORE the machine-identity ruling D1, because D1's recommended option only exists if MinIO and OpenFGA can trust the k3s ServiceAccount issuer, and a must-fail case with another ServiceAccount's token is what shows that trust is not 'any pod'. The residue deletions (P5.4-P5.6) are the parts of LH-016/LH-164/XC-075 that do not wait on D6 or D10.
- **P5.1** [sts; effort=a_day; ruling=False; closes=NEW medallion discards the table-scoped write credential; deps=P0.5]
  The medallion signs with what it is vended. authorize_stage_write returns the credential instead of discarding it. A per-table cache (the VendedCredentialCache pattern), with a write-tier entry for the destination and a read-tier entry for the upstream, feeds seed_bronze, the media ingest, read_upstream, measure_stage/existing_row_count and the InProcessExecutor. A server_mediated answer fails the stage and names why, with no ambient fallback. List what stays on the static key until P8.9: project_root/project_gold_root control reads and _lineage_outbox staging. Rewrite the RustFS/mode_b docstring.
- **P5.2** [sts; effort=hours; ruling=False; closes=NEW maintenance rewrites fall back to the static key; deps=P3.7]
  Maintenance fails closed. Read the record_credential_tier{tier="ambient"} series first, then replace the three `return fallback` arms in services/maintenance/src/maintenance/services/credentials.py with MaintenanceDenied carrying denial_remedy. State in the row that compaction now requires vending.mode sts or web_identity, and that under mode_b (still the code default) maintenance refuses every rewrite by design.
- **P5.3** [sts; effort=hours; ruling=False; closes=none (feeds D1, D7); deps=none]
  Live probes, read-only apart from one test token. (a) Can MinIO and OpenFGA fetch the k3s SA issuer's discovery document and JWKS (system:service-account-issuer-discovery bound for unauthenticated, kube CA trusted)? (b) Does AssumeRoleWithWebIdentity with an inline Policy enforce the intersection: in-scope GET allowed, cross-table GET and read-tier PUT denied? (c) MUST-FAIL: is a token from a DIFFERENT ServiceAccount with the same audience refused once the policy is bound by `sub`? (d) Can the catalog's cached namespace connections refresh before expiry? (e) Does GreptimeDB's object-store client have a web-identity or refreshing-credentials loader on MinIO? Pre-register the reading rule for each probe. Their outcomes feed D1 and D7.
- **P5.4** [sts; effort=under_an_hour; ruling=False; closes=XC-075 (compose clause); deps=none]
  XC-075: delete the minio services, or the files outright, from .docker/docker-compose.yml and docker-compose.rustfs.yml. docker compose is banned and nothing runs them. Removing only the image lines would leave invalid services behind.
- **P5.5** [sts; effort=under_an_hour; ruling=False; closes=LH-016 (nested half); deps=none]
  LH-016 partial. Re-list lakehouse-wh first, since the inventory is from 2026-09-24. As the admin bearer, DropTable only the NESTED copies lakehouse$silver-media$features and lakehouse$silver$features, never with a prefix delete. Keep silver-media$features and its binding: the chart's default media lane writes it (values.yaml:1520), and unbinding would break that lane until D6 gives it a home.
- **P5.6** [sts; effort=under_an_hour; ruling=False; closes=LH-164 (registry half); deps=none]
  LH-164 part 1: reap the 15 lance-catalog/medallion/models/e2etrain* registry datasets and their artifact bases, keeping churn. This is an object-store prefix delete BY NECESSITY, because no catalog door deletes a model. Anchor each prefix with a trailing `/`, take a dry-run listing with the same pattern first, and list the FGA tuples and lineage nodes it leaves behind (the residue LH-144 counts).

## Phase 6: boundaries and chart hygiene (no ruling)
WHY: The cadence change (P6.1) comes first because it cuts the worker's growth driver roughly 30x and the planner load it measures. LH-183's recycle proof (P6.2) is then observed on its own converge, so the two changes are not confounded. The port work (P6.3, P6.4) is self-contained medallion refactoring. The NetworkPolicy flip (P6.6) needs webhook ingress allows first, or pod admission fails for every pod the Dapr injector matches.
- **P6.1** [boundaries; effort=hours; ruling=False; closes=LH-195; deps=none]
  LH-195: when maintenance.workTopic is set (it is by default), the sweep cron defaults to the wall-clock `0 0 * * * *` that values-prod already uses, not @every (that interval resets on every sidecar start, and the planner pods lived 0.05-2.73 h). Extend the existing gate's _schedule_seconds to read that one form. Rewrite values.yaml:1544-1557 and :1576-1578, which still say the event lane is OFF by default. Deploy, then observe hourly planner ticks and a fresh write maintained through /maintenance-arrival. Close on the row's second branch: at backstop cadence, whole-estate re-planning is the declared design.
- **P6.2** [boundaries; effort=hours; ruling=False; closes=LH-183 (evidence only; close is D12); deps=P6.1]
  LH-183: record the 34.5 h verdict (planner plateau PASS, worker slope FAIL at about +17-19 Mi/h, native, with Lance session and Python heap flat). Prove the memory recycle fires with one converge setting recycleAtMemoryFraction=0.08: observe maintenance_worker_retiring with the memory reason, a clean replacement and no lost units. Restore by REMOVING the key from the stored user values (--reset-values with the live values file minus that key); do not --set 0.7, which would pin it forever. Read back `helm get values` (key absent) and the pod env (0.70). Re-measure the slope at the hourly cadence. The close waits on D12.
- **P6.3** [boundaries; effort=hours; ruling=False; closes=NEW train lane submits to Ray around the Executor port; deps=P0.5]
  Train lane behind the Executor port. Add an engine-neutral SubmitOutcome.ALREADY_FAILED plus a resubmit-terminal flag. RayJobsApiExecutor honours it and derives train_submission_id from the order's idempotency key. handle_train_trigger goes through executor_for(RAY_ENGINE). Done when train.py imports neither ray_submit nor ray_jobs_api and a live /train still dispatches and is watched. Rewrite DECISIONS.md:1465-1474 in the same commit: rayjob_executor was deleted in b3a10799. The Ray Jobs adapter extraction proposed alongside this stays a design note, not a row.
- **P6.4** [boundaries; effort=hours; ruling=False; closes=NEW workflow port covers only start/exists; deps=none]
  SagaClient gains `state` (an engine-neutral status enum), `terminate` and `signal(instance_id, event, data)`, implemented in dapr_saga.py. promotions.py moves its start, raise_workflow_event and WorkflowStatus use onto the port and deletes _exists. stage_ops.py and api/train.py use the port for state and terminate. Only then add promotions.py to the existing _ACTIVITY_MODULES list.
- **P6.5** [boundaries; effort=a_day; ruling=False; closes=XC-061; deps=none]
  XC-061: delete the security.infraContexts switch and render age, openbao, dex and minio hardened unconditionally, with the container-only keys and emptyDirs for the paths each writes. Replace the nats-stream Job's nats-box CLI with a first-party nats-py script (nats-py is already in the root lock) run non-root with the full baseline, so the ratchet's exception list ends up empty. Prove each one live, not by Running: an S3 PUT, an unseal plus a secret read through the Dapr store, a dex login, an AGE query, and a stream created.
- **P6.6** [boundaries; effort=multi_day; ruling=False; closes=XC-009; XC-017 §B5; deps=none]
  XC-017 §B5 via XC-009. Correct values.yaml:747-761: k3s enforces NetworkPolicy through its embedded kube-router, and this host's k3s runs without --disable-network-policy. Before enabling: add ingress allows from the API server to every webhook server in the namespace (dapr-sidecar-injector is failurePolicy Fail; also the remaining Kueue and CNPG webhooks), select the OpenFGA pod by app.kubernetes.io/name: openfga (its component label is authorization-controller), and check every Job and CronJob's labels. Enable it, then prove a new pod gets its daprd injected and the fleet reaches every store. Then scope Dapr accessControl across HTTP invoke, ActorProxy and Workflow (DECISIONS.md:1693).
- **P6.7** [boundaries; effort=hours; ruling=False; closes=NEW trash purge blocked by one orphan .txn (or hands it to D13); deps=none]
  Purge block: read the one orphan .txn's reclaimable_by_lance class in orphans.py. If it is below the listing floor, it clears itself once it is 7 days old: record that and wait. If it is above the floor, no format reclamation can take it at any older_than, and the choice goes to the owner (D13). Lance's default cleanup never removes unverified (aborted-commit) files.

## Phase 7: the chart release. What was fixed, what was not, and the first ruling to ask today
WHY: This answers the user's questions directly. WAS THE CHART PROBLEM SOLVED? The outage was: revisions 239 and 240 were written, and the release is at 96.3% with 38.8 KB free. The CAUSE was not. HOW CAN HELM HIT A LIMIT? Helm stores every revision as ONE Kubernetes Secret (manifest, templates, files, hooks, values), and the API server hard-caps any Secret at 1,048,576 bytes. It is not tunable, and --history-max limits how many revisions are kept, not how big one is. What fills it is rask's own 63 templates (~50%, including about 299 KB of template comments paid on every revision) and Kueue's templated CRDs (~24%). It has hit the cap four times: 2026-08-15, 08-18, 09-21 and 09-24. DOES RASK ASSUME KUEUE, OR DOES KUBERAY NEED IT? Neither. KubeRay has no batchScheduler set anywhere in the chart, no rask template sets kueue.x-k8s.io/queue-name, medallion.kueueQueue is empty with no reader, and rask's own `rask` LocalQueue has admitted 0 workloads. WHY DOES RASK KNOW ABOUT IT? Kueue used to be a separate release installed by scripts/k3s-install.sh. Commit 9f28f3d7 (2026-06-23) folded all cluster infra into the umbrella chart, and Kueue came along, sized as 'how many projects transcribe at once', which is one workload's GPU admission sitting in a platform seam. Its only live users are 8 Workloads in `htr-batch`: a namespace created 2026-07-27 with its own kueue-system v0.19.0 controller (not a helm release), its own rustfs, uv4-viewer and htr-reconciler, and 2 admitted jobs that are in ImagePullBackOff. Those Workloads sit on 11 CRDs that rask's release owns, with conversion pointing at default/rask-kueue-webhook-service and no resource-policy: keep. That is why kueue.enabled=false on its own would delete them. IS THE DESIGN CORRECT? No, in two ways. Operators do not belong in the app release (Helm's own CRD guidance; CNPG's CRDs already moved out, and ESO is already its own release). And the release object doubles as a store of every dev credential (Phase 4 removes that). A generic Helm skill would not have helped: the one read today covered none of this. The estate-grounded rask-helm skill written today holds the right model and needs only P0.6's correction. Order inside this phase: annotate keep, hand over, verify, then disable. Never flip kueue.enabled before the first two are read back.
- **P7.1** [chart; effort=hours; ruling=True; closes=NEW operators ride inside the app release (step A); XC-049 (Kueue half); CP-047; CP-012 (Kueue clause); deps=D11, P0.6]
  Kueue out of the rask release (D11). (1) kubectl annotate all 11 Kueue CRDs with helm.sh/resource-policy=keep. (2) Hand them to a real release owner: `helm install kueue oci://registry.k8s.io/kueue/charts/kueue --version 0.19.x -n kueue-system --take-ownership`, invoked through scripts/helm.sh from a repo target (or by htr-batch's owner, per D11). Conversion must then point at kueue-system/kueue-webhook-service (5 of the 11 CRDs have a conversion webhook). (3) Verify that the htr-batch Workloads read at both v1beta1 and v1beta2. (4) kueue.enabled=false. Delete templates/kueue-queues.yaml, the Kueue half of gpu-coherence.yaml, the _helpers.tpl references, the CA-restore init container and its test, and medallion.kueueQueue (not `ray.`). Delete the hook-applied rask ClusterQueue, LocalQueue and ResourceFlavor explicitly, because they carry no Helm annotation. Fix the engine_registry docstring. (5) Measure the revision with the Go-equivalent packing before and after; about 24% is expected back.
- **P7.2** [chart; effort=multi_day; ruling=True; closes=NEW operators ride inside the app release (step B); deps=P7.1, D11]
  Infra/app split (D11 option c, the end state DECISIONS 2026-08-15 already calls correct). Move nvdp, kuberay, nats, dapr, cnpg, openfga, greptimedb and perses into an infra release, each operator's CRDs annotated keep before handover, and have k3s-up do two installs. The app release stores no credential (after Phase 4). Cite the XC-054 gate and its declared blind spot (chart.files) in the row.

## Phase 8: ruling-gated work
WHY: Each step waits on one decision below, and the decisions are ordered by rows unblocked. Machine identity (D1) goes first after the chart: it unblocks OpenFGA authn, the web-identity parent, the workload credential door, the backup jobs and LH-129. Its prerequisite probes are P5.3. The default-lane decision (D6) closes three rows at once. The NATS broker auth waits on Kueue coming out (P7.1) for headroom, because it touches every pubsub component plus ESO objects and Jobs.
- **P8.1** [authz; effort=hours; ruling=True; closes=LH-076; deps=D2, P1.2, P2.2]
  LH-076 tenant creation (D2). Point POST /v1/projects at can_create_project in the ruled shape: under (a), can_create_project becomes admin or operator or owner and the model.fga.yaml operator/owner cases are updated. Regenerate model.json.
- **P8.2** [authz; effort=hours; ruling=True; closes=LH-056 clause 1; NEW write-tier vend exceeds the FGA rungs (tree/* half); deps=D3, P2.7, P2.8]
  LH-056 branch write (D3). Under (b), the main write vend drops tree/*, and a branch vend additionally requires can_create_branch in credentials.py and on the DescribeTable branch path (P2.8). Under (a), record it in DECISIONS.md only. Strike the targeted-event clause: the ref-plane events shipped untargeted under the codified rule.
- **P8.3** [authz; effort=a_day; ruling=True; closes=LH-178; LH-056 clause 2; deps=D4]
  LH-178 erasure against a pinning branch (D4). Implement the ruling in erase(). Its announcement is a TARGETED control event to the table owners (not 'the creator', which Lance does not record), which needs a new ControlAction and NotificationReason. Then build per-branch trash/undrop on create_branch(reference=...) in the same shape.
- **P8.4** [authz; effort=a_day; ruling=True; closes=LH-063; deps=D5]
  LH-063 principal key (D5). Change the token verifier that fills `sub` on the verified claims; that is the seam, not current_subject (140 token.sub reads in 32 files depend on it). Re-key the existing user:<sub> tuples, the notifications InboxActor ids, and the subject stamped in lineage onBehalfOf. Current state is test data, so a reseed is acceptable. Test that a connector rename keeps grants intact.
- **P8.5** [sts; effort=a_day; ruling=True; closes=LH-164; LH-194; LH-016; deps=D6, P5.1]
  Default lane and cascade head (D6). Under (a): bind bronze, silver, gold, bronze-media and silver-media to a platform warehouse instead of tenant ones. produce and media-produce create the head through the catalog with CreateTable ?mode=exist_ok (as ensure_stage_output does) and seed at the returned location. The produce.py:187 gate becomes 'catalog placed it'. Move train's _stage_base off bronze_uri, then remove MEDALLION_BRONZE_URI, MEDIA_BRONZE_URI, FROM_URI and TO_URI from the chart. Unbind silver-media from lakehouse-wh. Re-run the reconcile and see zero ungoverned medallion datasets.
- **P8.6** [sts; effort=hours; ruling=True; closes=LH-079; deps=D1, P5.3]
  Machine identity (D1), foundation. Create one ServiceAccount per service (today catalog, lineage, maintenance, medallion-producer, dex and ray-lance-head share `default`, and gateway, ingest, notifications and the 7 web-* zones share `rask`). Bind service-account-issuer-discovery. Record LH-079 in DECISIONS.md as bearer-only with no x-api-key, rewriting audit B6 and §8 item 3 as a conformance note citing spec.yaml's `security` disjunction.
- **P8.7** [authz; effort=a_day; ruling=True; closes=NEW OpenFGA API accepts any pod with no credential; deps=P8.6, P6.6]
  OpenFGA store closure. Set playground.enabled=false and authn.method=oidc against the SA issuer, restricted to the per-service subjects. Every FGA client (make_client, write_model, the bootstrap-admin and openfga-model jobs) presents a projected token with audience openfga, re-read from its file. Give OpenFGA its own Postgres role scoped to the openfga database, delivered by ESO as a mounted file, instead of the shared `lance` role through secretKeyRef env.
- **P8.8** [sts; effort=multi_day; ruling=True; closes=NEW vendor's own parent is a static key; deps=P8.6]
  Web-identity parent for the vendor. Register the SA issuer as a MinIO OpenID provider with a claim-bound policy (by sub, not readwrite-to-any-token). The catalog's vending AND its own IO use AssumeRoleWithWebIdentity(own projected token, session policy); the own-IO policy carries the observability and control deny. RoleSessionName is the sanitized caller subject. Rewrite the three falsified RustFS docstrings in vending.py.
- **P8.9** [sts; effort=multi_day; ruling=True; closes=NEW six static per-service MinIO users; NEW backup jobs sign as the tenant root; LH-129; CP-001; NEW Ray head holds every stage runner's signing key (remainder); deps=P8.8, P5.1, P5.2]
  Workload credential door: POST /management/v1/workload/credentials. It takes no path (the outbox door's precedent) and returns the spec's flat storage_options with expires_at_millis inside. It maps the caller's projected token to the policy documents moved verbatim from minio-scoped-users.yaml. Move catalog, medallion (control reads and outbox staging too), maintenance, lineage and both backup jobs onto it: the backups get per-run prefix-scoped sessions, and pg_dump uses PGPASSFILE. Delete those users, their derivations, their OpenBao seeds and the *_DAPR_SECRET_S3_FIELD wiring, and shrink minio-scoped-users.yaml. rask-ray-compute moves with LH-129, where each Ray job fetches its own credential by RASK_CREDENTIAL_REF plus its projected token file (never runtime_env or env); staging datasets come through DeclareTable vend_credentials. That also ends the Ray head holding peer signing keys. rask-viewer stays last (LOW). Measure the headroom recovered.
- **P8.10** [secrets; effort=hours; ruling=True; closes=LH-161; deps=D7, P5.3]
  GreptimeDB (D7). If P5.3(e) finds a refreshing-credentials path, use it through args and extraVolumes, with no subchart fork. If not, apply the ruled end state, for example [storage] type=File on its PVC. Either way the ROOT pair leaves the pod, and the _ENVFROM_EXCEPTIONS entry is removed in the same commit.
- **P8.11** [secrets; effort=hours; ruling=True; closes=XC-001 (infra half); deps=D8, P4.9]
  Rotation for boot-binding consumers (D8): MinIO, OpenFGA and the Collector. Under the recommended (b), a CronJob with get on the three ExternalSecrets and patch on the three workloads, reading no Secret, stamps each ExternalSecret's syncedResourceVersion onto its consumer's pod template. Write the AGE rotation procedure: ALTER ROLE, then the OpenBao write, then restart the clients.
- **P8.12** [sts; effort=hours; ruling=True; closes=LH-177; deps=D9, P2.10]
  LH-177 (D9). Under (a), record in DECISIONS.md that vending is in-cluster, and have the door annotate or refuse a caller it cannot serve, as closes-when requires. Off-cluster callers use vend_credentials=false and the server-mediated path.
- **P8.13** [sts; effort=a_day; ruling=True; closes=XC-075; deps=D10, P8.9]
  XC-075 store image (D10). Under (a), a .dagger/images.go function builds minio from a pinned upstream tag, values.yaml:2313 is repointed, and the Dagger lanes move from RustFS to the store the chart deploys, so STS enforcement is proven on the same binary. `mc` disappears with the scoped-users hook in P8.9.
- **P8.14** [provenance; effort=multi_day; ruling=True; closes=NEW NATS broker accepts unauthenticated clients; deps=P7.1, D14, P3.5]
  NATS decentralized JWT auth. The operator and account JWTs are public values; each app gets a user with publish/subscribe permissions enumerated first from /v1.0/metadata, $JS.API.> included. Sidecar apps get user JWT and seed via auth.secretStore on each pubsub.jetstream component; the provision/reconcile Jobs get them via an ESO file mount. No token auth, because that puts the secret in the server's env.
- **P8.15** [boundaries; effort=multi_day; ruling=True; closes=LH-197; XC-048; XC-017; LH-074; deps=D14]
  Batch-confirmed items (D14). LH-197: ARG SERVICE lakehouse dockerfile with a shared deps-only heavy layer (packages/ray-cluster-env precedent), per-member lakehouse-<svc> stems derived from one list across lance.catalogImage, k3s pins, K3S_IMAGES, .dagger and scripts, and added to the report-only scan matrix; show a lineage-only roll leaves the other ten untouched. XC-048: the DECISIONS §9 paragraph (trace context is the cross-hop correlation; X-Request-ID is an edge echo; the actor rides the payload as a claim). XC-017: rewrite closes-when. LH-074: maintenance reports last-known bytes per warehouse through a catalog management door it calls as its service identity (never relaxing its own _warehouses deny); a /management/v1/warehouse/{id}/quota sub-resource; PermissionDeniedError at write-tier vends, create/declare, the /commit door and server-mediated writes, with the overshoot bound stated.
- **P8.16** [boundaries; effort=a_day; ruling=True; closes=LH-183; deps=D12, P6.2]
  LH-183 close per D12. Under the recommendation, run one native-allocation profiling session (jemalloc or heaptrack on a worker) to name the library that retains about 1.06 KiB per no-op dataset-operation, file it upstream, then close with the recycle as the recorded bound.

## OWNER DECISIONS
- {"question": "D11 (ask first). Where do the cluster operators live, starting with Kueue? rask does not use Kueue: KubeRay sets no batchScheduler, no rask template names a queue, and rask's LocalQueue has admitted 0. Yet rask's release owns the 11 Kueue CRDs that the foreign htr-batch lane (8 Workloads, its own kueue-system v0.19.0) runs on, and those CRDs cost about 24% of the 1 MiB release ceiling. Owner-held fact needed: who owns htr-batch and kueue-system?", "options": "(a) rask ships no Kueue. After resource-policy: keep, the CRDs go to a Kueue helm release in kueue-system installed from a rask make target (like k3s-eso). (b) rask ships no Kueue, and Kueue is handed wholly to whoever owns htr-batch; rask's repo carries no Kueue at all. (c) (a) or (b), plus the full infra/app split: nvdp, kuberay, nats, dapr, cnpg, openfga, greptimedb and perses as their own release(s). (d) Keep the umbrella and shrink rask's templates by moving their comments to values.yaml or docs.", "what_each_costs": "(a) Hours, one careful CRD handover, and rask keeps maintaining an operator it does not use. Headroom goes from 38.8 KB to roughly 290 KB. (b) The same hours and the same headroom, and rask stops carrying another project's scheduler. It needs htr-batch's owner to take the release. (c) Adds a multi-day migration: ownership re-annotation of live objects and two installs in k3s-up. It is the end state DECISIONS 2026-08-15 already calls correct, and it ends the ceiling as a recurring outage. (d) No infra change, but it is a retroactive prose pass against the forward-only comment ruling, and Kueue's 24% stays.", "recommendation": "(b) if htr-batch is not rask's, which the evidence says (created 2026-07-27, own rustfs, viewer and reconciler, HTR-shaped), otherwise (a). Do it now, then schedule (c) once the post-Kueue revision is measured. Never flip kueue.enabled before the keep annotation and the handover are read back.", "rows_unblocked": "NEW operators ride inside the app release; XC-049 (Kueue half); CP-047; CP-012 (Kueue clause); and, through headroom, P4.x chart additions and the NATS auth row"}
- {"question": "D1. What credential does a MACHINE (service, Job, Ray job) present to the catalog, to OpenFGA and to the object store's STS?", "options": "(a) Projected ServiceAccount tokens (workload identity federation): audience-bound and kubelet-rotated, with one ServiceAccount per service, trusted through the k3s SA issuer by the catalog, OpenFGA and MinIO. x-api-key is never accepted. The service-token-* keys remain only as lineage HMAC keys. (b) Dex client-credentials: one client secret per machine, delivered from OpenBao (sidecar) or an ESO file (Job). (c) Management-API-minted scoped, expiring API keys (the Q7 x-api-key principal). (d) Status quo: static dedicated service tokens as bearers.", "what_each_costs": "(a) Multi-day: per-service ServiceAccounts (today 6+ services share `default` and 10 share `rask`), issuer-discovery RBAC, CA trust in MinIO and OpenFGA, and clients re-reading token files. It depends on P5.3's probes, including the must-fail case. No long-lived machine secret remains anywhere, which is §B's 'one bootstrap credential' posture. (b) Days. Each machine holds a long-lived client secret, and Dex's own client secret is a chart value today. (c) Multi-day plus a new key store, rotation and revocation design, and a store of long-lived bearer secrets. (d) Zero now. Static bearers forever, which the rule forbids.", "recommendation": "(a), conditional on P5.3 passing. If MinIO or OpenFGA cannot trust the SA issuer, fall back to (b) with secrets from the store, never a chart value.", "rows_unblocked": "LH-079; NEW OpenFGA API accepts any pod; NEW vendor's parent is a static key; NEW six static MinIO users; NEW backup jobs sign as root; LH-129; CP-001; NEW Ray head holds every signing key"}
- {"question": "D6. Where does the project-less DEFAULT cascade lane live, and does the cascade head ask the catalog for its location or tell it one? Today its top-level namespaces (bronze, silver, gold, silver-media) are bound into TENANT warehouses (lakehouse-wh, bind86-wh), and the head RegisterTables a composed {root}/medallion/<ns> path before any bytes exist.", "options": "(a) A named platform warehouse holds the default lane's namespaces. The head creates through the catalog (CreateTable ?mode=exist_ok, as every stage runner does) and seeds where the catalog places it. (b) No project-less lane: every cascade run belongs to a project, and the chart's default stage runners get one. (c) Keep the told location, and add a registrar-scoped unwind: the same service identity may deregister what it registered, within the request's idempotency window, while no Lance version exists.", "what_each_costs": "(a) About a day. The head gives up its deployment-contract location. A failed seed leaves a governed EMPTY table that the retry fills, so LH-194's closes-when is restated to 'no record governs absent bytes'. It removes four chart URIs and makes bronze readable by table_id_from_location. (b) About a day plus chart and e2e changes. It is cleanest for 'project > warehouse > namespace > table', but changes the chart's out-of-box lane. (c) Hours. A narrow, time-boxed exception to the 2026-09-10 ruling that a machine may not deregister what it registers. The cascade keeps its second home (LH-164 stays half-open) and stays on the RegisterTable location-trust path.", "recommendation": "(a)", "rows_unblocked": "LH-194; LH-164; LH-016"}
- {"question": "D2. Who may create a tenant (POST /v1/projects)? The model says `admin or operator` (model.fga:142), but the live store holds no admin or operator tuple on the estate root, and the migrator deliberately refused to promote owners to admin.", "options": "(a) Estate owner: can_create_project becomes admin or operator or owner. (b) Admin or operator only, which means granting the live owner estate#admin first. (c) Machines only, through `operator`.", "what_each_costs": "(a) Minutes of model work. It preserves today's behaviour, because the door checks owner now, and makes it explicit. (b) Reverses the migrator's recorded refusal, handing an owner two authorities nobody granted. (c) Nobody can create a tenant until an operator identity exists, and D1 must land first.", "recommendation": "(a)", "rows_unblocked": "LH-076"}
- {"question": "D3. May any holder of can_write_data (or can_maintain) on a table write EVERY branch of it? Today the main write vend covers tree/* and _refs, so branch isolation protects only against accidents.", "options": "(a) Yes, and record it. (b) No: branch writing is owner-tier, the same rung as can_create_branch; the main vend drops tree/*, and a branch vend requires can_create_branch. (c) Only the branch's creator.", "what_each_costs": "(a) No code. Branch isolation stops only accidents, not colleagues. (b) One policy change plus one rung check. Writers work on main unless they own the table, which is consistent with only owners creating branches. (c) Lance records no creator, and the control feed has retention, so it is not authoritative. It would need the per-branch record the 2026-09-21 ruling refused.", "recommendation": "(b)", "rows_unblocked": "LH-056 clause 1; NEW write-tier vend exceeds the FGA rungs (tree/* half)"}
- {"question": "D4. When a branch pins the version holding an erased subject, what does erase() do? A branch's creator is not recorded, so the people told are the requester and the table owners.", "options": "(a) Delete the branch, recreate it at main's post-erasure head with create_branch(reference=...), and notify the table owners. (b) Leave it: erase() stays complete=False and the residual goes to the requester and table owners as a targeted notification. (c) (b), then (a) automatically after a grace period unless the branch has been deleted.", "what_each_costs": "(a) The branch's divergent rows are lost unless re-applied, and a re-apply must never restore subject-derived rows. Needs a new ControlAction and NotificationReason. (b) Completion rests on a human with no deadline, and checkout_version(N) stays readable meanwhile. (c) Both mechanisms plus a scheduled step. The owner sets the grace period.", "recommendation": "(c), with the grace period set by the owner. An erasure must complete without depending on a human, and (a) alone destroys work without warning.", "rows_unblocked": "LH-178; LH-056 clause 2 (per-branch trash/undrop reuses the mechanism)"}
- {"question": "D5. What keys a person's grants? The FGA subject is Dex's composite `sub`, which packs connector id and user id, so even renaming the connector re-keys everything.", "options": "(a) Lakekeeper's shape: user:<idp-id>~<claim>, where idp-id is an operator-named stable id and the claim is the upstream user id via federated:id. (b) An internal stable principal (user:<ulid>) with an (issuer, sub) mapping registry. (c) Keep raw sub permanently and record that.", "what_each_costs": "(a) About a day or more. The seam is the token verifier that fills `sub`, plus a one-time re-key of the tuples, the notifications InboxActor ids and the lineage onBehalfOf subjects. A connector rename stops re-keying; a real IdP change still re-keys, by design. (b) Multi-day: a new registry, a lookup on every authentication, and a link/merge lifecycle and door. It survives any IdP change. (c) Zero now; every rename is a scripted migration.", "recommendation": "(a). Current state is test data, so a reseed is acceptable.", "rows_unblocked": "LH-063"}
- {"question": "D8. MinIO, OpenFGA and the OTel Collector bind their credential at boot, so an ESO rotation of rask-infra-credentials never reaches them. How are they restarted? (Under ESO the checksum annotation is constant, so dev rotation of minio.secretKey is broken today, not only in prod.)", "options": "(a) Stakater Reloader as its own release. (b) A narrow rask CronJob: get on the three ExternalSecrets and patch on the three workloads, reading no Secret; it stamps each ExternalSecret's syncedResourceVersion onto its consumer's pod template. (c) A runbook step (`rollout restart`). (d) Park until prod.", "what_each_costs": "(a) Hours and zero release bytes, but it adds a controller that list/watches and reads EVERY Secret in the namespace, a new reader of all credentials. (b) Hours. It reads no credential and its RBAC is limited by resource name. (c) Near zero, but a forgotten step leaves a dead credential while every probe stays green. (d) Leaves dev rotation broken now.", "recommendation": "(b)", "rows_unblocked": "XC-001 (infra half)"}
- {"question": "D7 (conditional on P5.3(e)). If GreptimeDB's object-store client has no refreshing or web-identity credential path, what is its storage end state?", "options": "(a) Move GreptimeDB to its PVC ([storage] type=File), with no credential at all. (b) A written exemption: one bucket-scoped static pair delivered as an ESO config file.", "what_each_costs": "(a) Hours. Telemetry loses S3 durability and capacity, and CLAUDE.md's observability line changes. (b) Hours, but it is a scoped static key, which the owner's rule says is not a fix. Offered only so the choice is explicit.", "recommendation": "(a). Never keep the tenant ROOT pair, as today. The backup jobs are NOT part of this decision: a minutes-long Job can hold a per-run AssumeRole session from the workload door (P8.9).", "rows_unblocked": "LH-161"}
- {"question": "D9. Is the in-cluster object store ever reached DIRECTLY by off-cluster clients?", "options": "(a) No. Vending is in-cluster by design; off-cluster callers use vend_credentials=false and the server-mediated path, and the door annotates or refuses a caller it cannot serve. (b) Yes. Publish MinIO at the edge with TLS, block /minio/admin, and vend a public endpoint per deployment and per warehouse.", "what_each_costs": "(a) Hours for the annotation or refusal the closes-when requires. The host-side lancedb/lance-ray conformance cases stay skipped by design (they pass in-cluster, 17/0). (b) Hours of code after P2.10, plus an internet-facing S3 data API.", "recommendation": "(a)", "rows_unblocked": "LH-177"}
- {"question": "D10. MinIO no longer ships an anonymously pullable image (minio/minio and minio/mc both refuse anonymous pulls, re-measured 2026-09-25), so a fresh node cannot start the store at all. Where does the store binary come from?", "options": "(a) Dagger builds minio from a pinned upstream source tag into a first-party image. (b) Mirror the bytes this node caches into a registry the estate controls. (c) Move to a store that ships pullable images and enforces session policies from a non-root identity.", "what_each_costs": "(a) About a day. The estate then owns rebuilds for MinIO CVEs, and the Dagger lanes can run the store the chart deploys instead of RustFS. (b) Hours, but frozen at RELEASE.2025-04-22 with no patch path, and only for hosts that reach that registry. (c) Multi-day. Every STS enforcement measurement must be redone, and RustFS was already dropped because its AssumeRole is root-gated.", "recommendation": "(a), as a supply-chain ownership choice (the Dagger rule fixes the build driver, not which third-party images must be first-party).", "rows_unblocked": "XC-075"}
- {"question": "D12. LH-183's worker fails its pre-registered slope rule, and the growth is native, outside both the Python heap and the Lance session. Does a memory recycle observed firing close the row?", "options": "(a) Yes. Close on the recycle plus hourly cadence, with the per-operation native retention recorded as a bounded residue. (b) No. Keep the row open until the holder is named.", "what_each_costs": "(a) Nothing more now, but CLAUDE.md allows a mitigation only as a labelled temporary step. (b) At least one profiling session (jemalloc or heaptrack), likely ending in an upstream pylance or Arrow report.", "recommendation": "(b), time-boxed to one profiling session. Once the library is named and filed upstream, close as (a) with the upstream issue cited. Do not close on a slope re-measured under the new cadence alone: that changes the condition the rule was registered under.", "rows_unblocked": "LH-183"}
- {"question": "D13 (only if P6.7 finds the orphan .txn ABOVE the listing floor). One unreferenced bronze/pages/_transactions/*.txn keeps the drift report unclean every tick, so no dropped table's bytes are ever purged. What is allowed?", "options": "(a) A narrow orphan-file reclaim door for files above the floor, limited to unreferenced _transactions/*.txn. (b) Exclude orphan_files from the purge gate, keeping it report-only.", "what_each_costs": "(a) Hours, and it is the first deletion of files the format itself will not reclaim. (b) Minutes, but the purge stops waiting on a class the estate deliberately does not act on.", "recommendation": "(b). The orphan scan is report-only by design because of five known false-positive classes, and it should not hold an unrelated reclamation hostage.", "rows_unblocked": "NEW trash purge blocked by one orphan .txn"}
- {"question": "D14. Confirm these recommended rulings as one batch, with exceptions if any. Each is strongly evidenced, but its row reserves it to the owner.", "options": "Confirm all, or confirm with named exceptions. (1) LH-197: one lakehouse image per workspace member (1 stem becomes 7, with a shared deps layer). (2) XC-048: trace context supersedes request-id propagation (live traces span 5 services); record it in DECISIONS §9. (3) XC-017: closes-when becomes 'every §B gap is closed or owned by a named row' instead of a checked list re-asserting judgements. (4) LH-144: the reconcile may record an observed-absence DROP (a DatasetEvent, only on a clean not-found). (5) LH-074: usage comes from maintenance's reconcile via a catalog management door, refused at issuance, create and commit, with overshoot bounded by one tick plus 900 s. (6) NATS broker auth and OpenFGA authn are NOT covered by the 2026-09-21 no-prod parking, because both are in-cluster bypasses of authorization; XC-006 and XC-007 TLS stay parked.", "what_each_costs": "(1) About a day and more registry and CI surface; it ends the freeze where one soak blocks all 11 deployments. (2) A paragraph. (3) Drops a gate that could only assert a document. (4) Hours. (5) About a day, low priority. (6) Puts two multi-day rows into the plan rather than parking them.", "recommendation": "Confirm all six.", "rows_unblocked": "LH-197; XC-048; XC-017; LH-144 residue; LH-074; NEW NATS broker; NEW OpenFGA API (timing)"}

## NO-CODE CLOSES
Close now by register edit, plus a DECISIONS.md line where noted: LH-148 (residue retired by retention, 0 of 278 role literals), LIN-002 (omission recorded for arrival-triggered runs), LH-041 (tag-move LWW recorded), XC-011 (content gate shipped, artefact has no consumer). Supersede now, with the code living in the replacing row: LH-077 and XC-020 go to the new transaction row, CP-047 to the chart/Kueue row. Close after one small non-code act: XC-002 (delete Secret/rask-ray-compute-s3 and align one test docstring and two skill passages), LH-099 (observe one compaction RunEvent, then record the decline), XC-048 (owner confirms D14, then the DECISIONS §9 paragraph). Stays parked, not closed: XC-006 (no-prod ruling). Not closable without code despite a domain plan's claim: LH-144 (dropped_at is a live regression), LH-183 (the worker failed its own rule), XC-017 (the §B gaps still need controls).
