# Contributing

See `README.md` at the repo root for the build / test loop.

## Commit style

Conventional commits — see `cliff.toml` for the changelog parsers.

## Adding a UI component

See `packages/oxen_componets/README.md`.
