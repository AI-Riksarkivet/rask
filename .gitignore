__pycache__/
*.pyc
.coverage
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
# uv / virtualenvs — `.venv/` matches at ANY depth (root + any stray projects/*/.venv)
.venv/
**/.venv/
.uv/
uv-cache/
.python-version-local
.env

# JS / SvelteKit
node_modules/
.svelte-kit/
storybook-static/

# Zensical docs build output
site/

# Local SQLite catalog + manifest dump (built by components/scripts/{build,sync,count}_*.py)
.cache/
batches.db
components/scripts/batch_ids.txt
batches.db.20260527T105358Z

# Turborepo
.turbo

# vendored helm subcharts — rebuilt via `make k3s-deps` (helm dependency build) from Chart.lock
chart/charts/
