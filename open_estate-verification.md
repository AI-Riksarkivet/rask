# Open: estate verification after the audit-drain campaign

Working plan. Delete when every row is CLOSED. Status is counted from this file, never asserted
elsewhere — the campaign that produced it was faulted for exactly that.

**Context.** A 300-finding Python-estate audit was drained over 2026-08-28/29 (~49 commits). An
adversarial re-audit of that drain found the code broadly sound and the ledger untrustworthy; the
ledger was reconciled (`open_python-audit.md`, now 118 OPEN / 119 FIXED, counted from rows). A
capability audit then verified the platform's core use cases and found the defects below. The estate
was redeployed from main (rev 86, 2026-08-29) after 101 commits of drift — before that, nothing
observed in-cluster described current code.

## Rows

| # | Row | Status | Note |
| --- | --- | --- | --- |
| 1 | `describe_table` bound spec BODY fields as QUERY params — lance-ray/lance-ns got no credentials | **CLOSED** | `4c64046c`. Verified over a real uvicorn socket, 10 request shapes (TestClient hides this class) |
| 2 | Ingest per-run S3 `endpoint` advertised and ignored — a same-named local bucket could serve the WRONG BYTES silently | **CLOSED** | `4c64046c`. Endpoint crosses the queue; creds from the registered store's Dapr secret; unhonourable source FAILS rather than reading the wrong bucket |
| 3 | Search FGA gate authorized `declared.search` while the handler read `?table=` | **CLOSED** | `4c64046c`. Latent (no descriptor declares two), but a grant on A authorized B. Tests assert the exact FGA object checked |
| 4 | `search/similar` 400'd on every integer-keyed corpus | **CLOSED** | `4c64046c`. Reuses `keys.py`'s cast; the duplication WAS the defect |
| 5 | Seven pre-flight DROP paths halted the cascade with no lineage — so notifications told nobody | **CLOSED** | `4c64046c` |
| 6 | Compaction refused EVERY external-base dataset, incl. the cascade's own tiers — fragments accumulate forever | **CLOSED** | `531864e2`. Three independent signals, fails closed; 6 wrong-permit constructions all refused; mutation-tested; payloads re-read in a cold subprocess |
| 7 | Producer could not reach the catalog on ANY shipped chart (`MEDALLION_CATALOG_URL` gated on `qualityReview`, default false) | **CLOSED** | `531864e2`. Hoisted unconditional + render assertion at both flag states. This estate runs `qualityReview: true`, so it was invisible HERE and total everywhere else |
| 8 | Maintenance whole-estate pre-pass ignored the operator's Lance cache cap | **CLOSED** | `4c64046c`. Source-level pin — at default config the sessions coincide, so no runtime assertion can see it |
| 9 | **`/ingest-media` writes bronze with NO catalog call** — identical defect to row 7, same service | **CLOSED** | Uncommitted. `media_produce.py` registers `bronze-media$objects` through the same `catalog_register` seam, before the first blob, fail-closed → 503. RED measured: 12 failed without it (9 in `services/medallion/tests`, 3 e2e in `tests/unit`), incl. `policy/set` 404 over a real catalog socket. Chart needed NO fix — `helm template` MEASURED the catalog trio already unconditional on the producer pod (row 7 hoisted it, and `/ingest-media` is served by that same pod); the new render assertion pins the half row 7's does not — that `MEDALLION_MEDIA_BRONZE_URI` stays registerable under `MEDALLION_CATALOG_ROOT`, which `medallion.buckets` zoning would silently break |
| 10 | Rebuild + redeploy rows 6/7/9 | **CLOSED** | 14 images at `main-90b8d0ed` via Dagger, zero failures; helm rev 87 deployed, 23/23 fleet deployments on it |
| 11 | e2e suites against current code | **MOSTLY CLOSED** | Run against rev 87. **medallion 5 passed (34.6s — the cascade actually ran)**, media 1 passed, gateway 3 passed, lineage 7 passed / 2 failed. FOUND AND FIXED: both cascade suites omitted the REQUIRED `Idempotency-Key` header, so they 422'd before reaching the door and had been asserting 202 against something unreachable — the estate's headline cascade coverage was illusory. The route's own docstring called the header "(optional)"; corrected. Media passing also confirms rows 9+10a: `/ingest-media` returns 202, not the 503 it would give without the namespace |
| 11a | 2 lineage e2e failures — the suite could not pass on a working estate | **CLOSED — fixed and verified live** | Three defects, none in the product. (1) The fixture seeded PRODUCTION dataset names (`silver$features`, `gold$catalog`) then asserted over every producer and every column of them, so real cascade writes made it red — a clean-room test wearing an e2e's name. `_Sample` now namespaces datasets AND run ids per module run; the run ids mattered too, being fixed uuids that collided with the suite's OWN previous runs so `run.outputs == [...]` accumulated. (2) `test_terminal_lifecycle` poisoned itself: fixed `lc-1/2/3` ids meant run #2 met run #1's 09:10 recreate outranking the 09:05 drop it had just written, so it could only ever pass once. (3) **The destructive guard was defeated by a port-forward** — it gated on the DSN HOSTNAME being local, which asks how you REACHED the database, not which one it is. `kubectl port-forward` makes production answer on localhost, and that is how this suite gets run against a cluster. Not theoretical: it ran that way on 2026-08-30, pruned the live lineage feed, and caused the ten-hour notifications error storm in row 23. Now an explicit `LINEAGE_E2E_DESTRUCTIVE=1` opt-in — an env var cannot be forged by a network path. **Verified against the LIVE estate, where it could not pass before: 8 passed / 1 skipped, and three consecutive runs identical** (it previously went 2-failed then 3-failed). The gate proven both ways: skips against the live DSN, opens under the opt-in pointed at a dead port |
| 11b | `test_maintenance_e2e` + a real sweep tick | **CLOSED** | Suite 1 passed. More usefully, I fired an actual `maintenance-cron` tick against rev 87: **68 datasets, `refused: 0`, `refusals: {}`**, 38 indices optimized, `trashed: 7` correctly excluded. That is the row-6 proof: the OLD gate refused every external-base dataset and the medallion tiers use `initial_bases`, so they were refused; they now sweep and get index work. `fragments_removed: 0` now means NOTHING EXCEEDS ITS PER-TIER TARGET, not "refused" — a different statement from the pre-fix zero. Physical compaction on that exact shape was proven by the fix's verifier against real pylance (4 fragments → 1, payload dir byte-identical, 20/20 blobs resolving in a COLD SUBPROCESS). NOT taken: a live reclamation measurement on a deliberately over-fragmented dataset. Marginal evidence over the two proofs above; worth doing if reclamation is ever doubted |
| 12 | Playwright visual pass + governance/lineage logs | **CLOSED** | Repeated against rev 87, all zones. Anonymous browse redirects to Dex; home renders the estate-level bar (Home/Projects/Settings) with Settings visible for an estate-admin; lakehouse catalog lists 4 pages of governed `acme-bronze$*` under an FGA-filtered header; **lineage graph LIVE, 55 datasets**, bronze→silver→gold plus `compaction/*` and `lance-reconcile/*`; explorer, compute and models clean; annotator renders 12 task cards with honest state including a `publish failed` badge beside a `published` one. Zero console errors. TWO FALSE ALARMS OF MY OWN, both chased down rather than reported: `/lakehouse/lineage/graph` 404 was my URL (the sidebar links to `/lakehouse/lineage`), and an annotator "error" was `404` matching inside a TIMESTAMP (`lease drive 1786912796404`) |
| 13 | `catalog/services/maintenance.py:61` tells the operator the sweep refuses flag 16 "for the same reason" — after row 6 the on-demand button is the STRICTER of the two | **CLOSED** | Verified with real pylance and a shipped-route smoke (`POST .../maintenance/compact` -> 200, 4 fragments -> 1). The on-demand door now gates on the same evidence the cron does; the false 'the sweep refuses it for the same reason' clause is gone. Mutation-tested: restoring the stale sentence fails its own test |
| 14 | `make openapi-check` red at HEAD — committed spec stale in 8 unrelated paths + 3 schemas | **CLOSED** | Spec + TS client regenerated; `make openapi-check` green. Verifier confirmed the diff is catch-up, not a hidden API change |
| 15 | ~118 audit findings remain | **CLOSED (count was stale)** | Re-derived from the table itself 2026-08-30: **2 OPEN / 43 PARTIAL / 224 FIXED / 11 OWNER / 20 DISSOLVED / 2 OBSOLETE / 2 WRONG = 304**. No HIGH is open. The two OPEN rows (`CAT-CORE-13`, `DUP-15`) are corrections to the drain's OWN false claims, not undrained findings. The 43 PARTIAL each name their surviving remainder |
| 16 | 11 owner decisions | **PARTLY CLOSED** | All eleven were briefed with each premise adversarially re-checked (Appendix H of `open_python-audit.md`); three premises were corrected by that re-check. The owner RULED on four — X10 (unify auth, HARD rename, no aliases), ingest-flow-12 (comment rule forward-only), PS-09 (delete `packages/tracker`), ANN-05 (fail closed, legibly) — and all four are implemented, adversarially verified, and shipped in `6897fa0e`. The remaining SEVEN have a stated default the owner did not object to, but are **NOT IMPLEMENTED**: ANN-13, CAT-CORE-04, ingest-flow-11 (the only one rated *should-decide-soon*), PS-07, catalog-api-17, MED-011, X1 |
| 17 | `lance-medallion/embed_features` + `aggregate_gold` FAILING repeatedly on the live estate | **CLOSED** | Root cause found by RUNNING a cascade rather than reading: `ray_stage_job.py` built the destination schema by *editing* the upstream's (strip `stage`/`lineage`, append them back at the END) while the transform EMITS them wherever `stamp_stage` puts them. lance_ray casts each appended block POSITIONALLY, so a re-stamp shape — silver, already carrying both — landed columns in the wrong slots and gold failed. Fixed to one construction (`_target_schema` over a zero-row slice of the real transform). Proven live end to end: silver→gold ran COMPLETE and the promotion published. My first two tests for it were tautological and were rewritten before the fix shipped |
| 18 | Those failures author as `data_eng` / `analyst` — chart ROLE LITERALS, not people | **CLOSED** | Cascade FAILs now carry an originator through the tier boundary. REJECTED on first attempt and fixed by me: the fix declared a local `_NOT_A_PERSON = {'', '*', 'user:*'}`, a weakened copy of `is_person_subject`, so `publication_originator('data_eng', ...)` still returned `data_eng` — the exact symptom. It now calls the one definition (whose own docstring says 'the two disagreeing is the whole failure mode') and strips `user:` BEFORE checking, since that prefix reads as an FGA object id. 13 cases verified; RED-proven by restoring the weakened set (6 role-literal failures). The guard test's parametrize gained the role literals it had omitted |

## Standing rules this work is held to

- A fix is not closed until a test FAILS without it. Several rows above were rejected the first time
  for being adjacent to the defect, or for a test that could no longer fail.
- **A fix that does not reach a shipped deployment is not a fix** (row 7 was rejected for exactly this:
  correct code behind a chart conditional that defaults off).
- Prose that a change falsifies is rewritten, not appended to. Five rejects in this campaign were
  docstrings left asserting the old behaviour.
- Counts are quoted only when personally run.
| 19 | Deletion semantics verified live | **CLOSED (mostly)** | Driven against rev 87 with throwaway fixtures under `lakehouse-wh`, then cleaned up. PROVEN: a non-empty namespace refuses **409 NamespaceNotEmpty**; a `protected` table refuses **409**; `?force=true` drops it **200**; describe then **404**; **undrop restores it 200** with a RELATIVE location (`e5e49a8d_…`, the #75 rule); describe after undrop **200** fully restored; purge + namespace drop both **200**. THE SECURITY PROPERTY HOLDS: a second identity (bob) gets **403 `can_drop required` BOTH with and without `force=true`** — force turns the protection lock only, the FGA gate runs first and identically. Post-drop describe returns 403 not 404 because the gate precedes existence resolution and the drop revoked the tuples. TWO NOTES: (a) `force` rides the QUERY STRING, not the body — a JSON `{"force":true}` is silently ignored, which cost me a false "defect" until I read `tables.py:810`; (b) the 409 reports *"contains 1 child objects"* — a COUNT, not the names, while `.claude/skills/rask-lance-catalog` says the refusal NAMES its contents. NOT yet verified: warehouse/project delete, cascade detach + plural undrop, and bucket-purge sole-ownership |
| 20 | "No active project" in the shell — **NOT A BUG** | **CLOSED** | Owner spotted it in a screenshot I had read past. The mechanism works as designed (#103 "opening a project IS entering it"): `/projects/<id>` stamps `rask_active_project`, and `zoneLayoutLoad` (`@rask/api/bff.ts:274`) reads it back for every zone. PROVEN live: cookie absent → shell says "No active project"; after `/projects/acme` the cookie is `rask_active_project=acme` and the annotator's shell reads "Acme · Project". My navigation went straight to `/annotator` and never entered a project. (My first per-zone grep counted 0 producers in six zones and looked like an estate-wide dangling prop — wrong, because the producer lives in the SHARED package, not in `microfrontends/*/src`.) |
| 21 | The `Project` CRD is not installed, so `/api/projects` can never list | **CLOSED — premise PARTLY REFUTED, cause named at the door** | The CRD is genuinely absent (re-verified live: `kubectl get projects.platform.rask.io` → *the server doesn't have a resource type*; 71 CRDs, 0 matching `project`). BUT (a) **the gallery never sourced from the controlplane** — `home/src/lib/gallery.ts:73` reads `/capi/v1/projects`, which `bff.ts::makeCatalogProxy` sends to the **catalog**, and memberships come from `me.projects`; that is also why entering `/projects/<slug>` works, so the asymmetry has no controlplane in it at all. (b) **The CRD must NOT be added to the chart** — `docs/DECISIONS.md` 2026-08-16 rules it abandoned as a rask-repo concern (it belongs to the separate `rask-operator`), because a CRD without its controller renders unreconciled CRs as projects stuck mid-provision. Neither option (a) nor a swallowed 404 was the fix. (c) What WAS broken and is fixed: the 404 was reported as `503 cannot reach kubernetes api` — a lie (the API server answered) that had already sent `HANDOFF-lakehouse.md:101-106` after the ServiceAccount and `OPEN-WORK.md` §G1 after shipping the CRD. It now answers **501** naming the unregistered `projects.platform.rask.io` type; an RBAC 403 and a refused connection keep the distinct 503, and an empty `200` is refused by its own test. `services/controlplane/tests/test_the_missing_operator_names_itself.py` (5 tests, 2 RED before the fix); §G1's prescription rewritten |
| 22 | The annotator picks its tenant by ARRAY POSITION | **CLOSED** | The annotator reads the same active-project source the shell does instead of `me.projects[0]` |
| 23 | The notifications reconciler logged 2 ERRORs every 30s for TEN HOURS | **CLOSED — fixed and deployed** | Found in the logs, not the code. 2026-08-29 22:00 → 2026-08-30 08:08: **1210 `lineage_feed_pruned_below_cursor` + 1210 `lineage_event_invalid`**, two per cron tick, from ONE pruned feed and ONE malformed row. The pod never restarted (15h, 0 restarts) — it healed only when fresh cascade traffic finally dragged the mark out of range. Both diagnoses were correct; the REPETITION was the defect. On an idle feed the mark cannot climb away from either condition, so every tick re-derived the same loss and re-announced it, making `record_feed_gap()` count one gap 1210 times (so any alert on that series is meaningless) and burying every other service's errors. Fix: the gap is ACCEPTED into the cursor's existing `floor` field ("rows this deployment deliberately skipped" — rows lineage deleted before we read them are exactly that), and a malformed row is reported at FIRST SIGHT only, since `FEED_OVERLAP` re-offers rows by design and a DROP is terminal. A LATER prune is still reported, pinned by its own test — a fix for noise that deafens the detector would cost more than the noise did. `reconcile` crossed C901 at 16 so its post-walk half was extracted to `_settle`; no threshold was raised. 3 RED tests, each mutation-proven. **Deployed and verified: 40 clean ticks, 0 errors** |
| 24 | EVERY durable activity ran on an event loop the previous one had closed | **CLOSED — fixed and deployed** | Both movers logged `Activity execution failed - task_id: 1, error: Event loop is closed` on **every stage dispatch** (28 in 24h on silver-to-gold) and then succeeded ~2s later on the retry. Reproduced standalone before touching anything: call 1 pools a keep-alive connection on loop L1, `asyncio.run` closes L1, call 2 inherits the corpse, call 3 succeeds because the failure evicted it — exactly the observed one-warning-per-dispatch shape. The design contradicted itself: `ray_submit` pools ONE `httpx.AsyncClient` for "the WORKER's lifetime" (deliberately, with a test) while `_run_async` gave each activity a fresh loop. Only two designs are consistent; this takes loop-per-worker, which is what the client already claimed. New shared `service_kit.activity_loop`, because **both** medallion and ingest carried the same bridge — ingest's docstring even argued there was "nothing to keep warm between invocations", the precise assumption medallion's pooled client falsifies. Shutdown had the mirror fault (`mover.py` closed the pool from the LIFESPAN loop) and now closes on the owning loop. RED at the real seam with a stdlib HTTP/1.1 server (keep-alive IS the mechanism — under 1.0 nothing pools and the bug vanishes); mutation-proven. **Deployed and verified live: dispatch 10:42:58.742 → submit 10:42:58.757, 15ms, zero warnings** (was ~2s + a warning, every stage) |
| 25 | Two non-application log sources remain | **OPEN — not rask code** | After rows 23+24 shipped, a 15-minute scan of every deployment shows **all nine rask services clean**. What is left: (a) `rask-kueue-controller-manager` — `http: TLS handshake error … remote error: tls: bad certificate`, ~450/min, a third-party operator's webhook cert; (b) `rask-otel-collector` — 2× `Failed to scrape Prometheus endpoint` (warn), a scrape target that is down. Neither is rask code and neither affects the cascade, but (a) is loud enough to hide things and should be chased |
| 26 | The generic stage lane rescanned the whole tier and forbade fan-out estate-wide | **CLOSED — fixed, deployed, verified live** | Owner pushback, and it was right: I had reported the 1:1 assertion as settled design. It was a defect. **Also a correction I owe the record:** my claim "the cascade cannot fan out" was OVERSTATED — the cluster bakes three jobs and a project can declare a lane naming any of them (`TransformSpec.entrypoint`, validated against `BAKED_CLUSTER_JOBS`), and `ray_dummy_job.py` already did CDF-delta + `merge_insert` correctly. The accurate claim is narrower: the DEFAULT lane every mover runs, `scripts/ray_stage_job.py`, hard-asserted `out.count_rows() == upstream.count_rows()` and never read `BASE_VERSION` (grep: zero matches, while `ray_submit.py:177` had exported it all along). Both fixed: the CDF predicate `_row_created_at_version > base` for the read and `merge_insert` on the tier's `id` for the write (Dapr delivers at least once; an append would double a redelivered delta), and `_assert_stage_contract` replacing the count equality. That replacement is a TIGHTENING — provenance completeness is asserted on every run, every cardinality, delta and full alike, and a transform that swapped two rows for two unrelated ones passed the old check and fails the new one. Wired end to end so the capability is reachable: constants defined once in `stage_stamp` → validated `TransformSpec.cardinality` → the catalog's `extra="forbid"` request AND response models → `ray_submit` exporting `STAGE_CARDINALITY` → the job. DEGRADES SAFELY: a delta against a destination that cannot merge falls back to a full run, because writing a delta into a table `_reset_if_legacy` just wiped is silent data loss — my own first test hit that path and I mistook it for a bug. **PROVEN ON THE LIVE ESTATE** after rebuilding both images and re-rendering the release: `stage=silver lane=full rows_in=8` (bronze-arrival carries no version range — correct) and `stage=gold lane=delta` off `BASE_VERSION=106`, with `CARD='1:1'` exported on both. A `lane=` field was added to the completion line first, because a delta run and a full rescan had logged identically — the one property the change delivers was the one an operator could not confirm |
| 27 | `LANCE_EXTERNAL_BLOB_BASES` rendered on the catalog only, so every ingest COPIED the corpus | **CLOSED — fixed, deployed, verified live** | `services.yaml` set it on the catalog; `fleet.yaml`, which renders ingest, set it nowhere, and the setting defaults to empty — so `approved_external_base()` returned None in the ingest pod on every shipped chart. The repo already carried the cost: **3,232 bytes external vs 4,002,901 managed on a 4 MB corpus** (`ingest/adapters.py`), with one warning log on a green run as the only signal. The ingest module's docstring stated the rule the chart made unkeepable. Rendered from the ONE `vending.externalBlobBases` value rather than duplicated into a values `env:` map — two literals is how the ingest side approves a write under a base the catalog's manifest never registered, and `initial_bases` is create-mode only, so that divergence is unrepairable on every table it lands on. RED-proven by rendering the chart and reading env off every Deployment (only `catalog` carried it); mutation-proven. **Verified on the live estate at helm rev 88: both `rask-ingest` and `rask-catalog` now report `s3://lance-catalog/models/`** |
| 28 | The X10 hard rename required a chart re-render, and the estate had to be walked through it | **CLOSED** | Deploying the new image alone put `rask-catalog` into CrashLoopBackOff — by design. The unified settings REFUSE the retired `LANCE_OIDC_*`/`LANCE_FGA_*` names at boot rather than falling back, so a pod whose Deployment still carried them failed loudly with the names listed and "Re-template the deployment" in the message. That is the ruling working (*"a missing variable must fail LOUDLY at startup, never silently fall back"*), not a regression — and no outage followed, because Kubernetes never promoted the new ReplicaSet and the old pod kept serving. Resolved with a `helm upgrade` mirroring `make k3s-up`'s invocation over a snapshot of the LIVE release's values with only the image tags patched — never a bare upgrade, which would have reverted every deployed image to the chart default. Release rev 88; auth confirmed still ON under the new names (`RASK_OIDC_ENABLED=true`, `RASK_FGA_ENABLED=true`); fleet 55 Running / 0 failed |
