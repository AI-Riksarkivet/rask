{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ra-hcp.fullname" . }}
  labels:
    {{- include "ra-hcp.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- if $.Values.frontend.enabled }}
          {{- /* Swagger UI & OpenAPI spec — direct to backend (public, no auth) */}}
          - path: /docs
            pathType: Prefix
            backend:
              service:
                name: {{ include "ra-hcp.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          - path: /openapi.json
            pathType: Exact
            backend:
              service:
                name: {{ include "ra-hcp.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- /* Everything else — including /api — goes to the SvelteKit frontend. */}}
          {{- /* The frontend proxies /api to the backend, injecting the Bearer */}}
          {{- /* token from the httpOnly session cookie. This is required because */}}
          {{- /* browser requests (img tags, XHR uploads, downloads) cannot add */}}
          {{- /* auth headers themselves. */}}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "ra-hcp.frontendFullname" $ }}
                port:
                  number: {{ $.Values.frontend.service.port }}
          {{- else }}
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "ra-hcp.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
