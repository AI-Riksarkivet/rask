{
	"//": "Extends the shared base, then re-inlines noUncheckedIndexedAccess. rask-frontend records that the six packages extending @rask/config alone are the LEAST strictly typed in the estate and calls that 'a defect, not a design' — a new package should not join them. exactOptionalPropertyTypes stays OFF: it is the one flag Svelte packages cannot carry (Bits UI 'union too complex'). `types` carries 'node' for the TESTS only: split.test.ts reads GroupActions.svelte off disk to prove the header imports the shared verb rule instead of restating it, which is the only way to inspect a component's source in a repo with no DOM test environment. Nothing under src/ that ships to a browser may import node:*.",
	"extends": "@rask/config/tsconfig.base.json",
	"compilerOptions": {
		"noUncheckedIndexedAccess": true,
		"noEmit": true,
		"isolatedModules": true,
		"verbatimModuleSyntax": true,
		"types": ["svelte", "node"]
	},
	"include": ["src/**/*.ts", "src/**/*.svelte"]
}
