node_modules
.turbo
microfrontends/*/build
microfrontends/*/.svelte-kit
.svelte-check
microfrontends/*/test-results
packages/*/dist
storybook-static/
.svelte-kit
test-results

# #143 evidence screenshots — regenerated by `e2e/settings/gated-action.spec.ts` on every run.
# Reviewed in the PR conversation, not stored: binaries that rot silently are worse than none.
microfrontends/*/e2e/__shots__
