<!--
	NOT A PAGE — this file exists only because a route needs a leaf component beside its `+page.ts`,
	and that `+page.ts`'s load always redirects, so nothing here ever renders.

	It used to read "The Admin zone (P0 scaffold). Routes move here from apps/web in P3." — a stale
	P0 note behind a redirect that had been there since the redirect landed. OPEN-WORK.md records
	these scaffolds as REMOVED, which is true of the behaviour and was never true of the files: a
	reader auditing this zone's surface found pages that cannot be reached and text describing a
	migration that finished.
-->
<svelte:head><title>rask — Admin</title></svelte:head>
